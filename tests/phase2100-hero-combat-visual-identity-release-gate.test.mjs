import test from 'node:test';import assert from 'node:assert/strict';
import { auditReleaseFreeze } from '../dist/game/release-freeze-audit.js';import { releaseCandidateAudit } from '../dist/game/release-candidate-audit.js';
test('phase 2100 release freeze binds hero combat visual identity evidence at sixty samples',()=>{const f=auditReleaseFreeze();assert.equal(f.heroCombatVisualIdentityAssetsPassed,true);assert.equal(f.heroCombatVisualIdentityAssetsSamples,60);assert.equal(f.passed,true);});
test('phase 2100 candidate fails closed on forged hero combat visual evidence and sample mutation changes signature',()=>{const base=releaseCandidateAudit();assert.equal(base.status,'PASS');const forged=structuredClone(base.evidence);forged.releaseFreeze.heroCombatVisualIdentityAssetsPassed=false;forged.releaseFreeze.passed=true;const rejected=releaseCandidateAudit(forged);assert.equal(rejected.status,'REVIEW');assert.ok(rejected.issues.includes('release-freeze'));const changed=structuredClone(base.evidence);changed.releaseFreeze.heroCombatVisualIdentityAssetsSamples+=1;assert.notEqual(releaseCandidateAudit(changed).signature,base.signature);assert.match(base.markdown,/hero-combat-visual-identity-assets safe \(60\)/);});
