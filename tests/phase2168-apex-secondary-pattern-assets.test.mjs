import test from 'node:test'; import assert from 'node:assert/strict';
import { APEX_SECONDARY_PATTERN_ATLAS,APEX_SECONDARY_PATTERN_IDS,apexSecondaryPatternIcon,auditApexSecondaryPatternAtlas } from '../dist/game/apex-secondary-pattern-identity-assets.js';
test('phase 2168 provides six static APEX secondary pattern identities',()=>{assert.equal(APEX_SECONDARY_PATTERN_IDS.length,6);assert.deepEqual(APEX_SECONDARY_PATTERN_ATLAS,{src:'./assets/bosses/apex-secondary-pattern-icons.png',columns:3,rows:2,cellSize:96,width:288,height:192});for(const id of APEX_SECONDARY_PATTERN_IDS){const icon=apexSecondaryPatternIcon(id);assert.equal(icon.archetype,id);assert.equal(icon.spawnToastIdentitySupported,true);assert.equal(icon.persistentRecallIdentitySupported,true);assert.equal(icon.secondarySpecialIntentSupported,true);assert.equal(icon.animated,false);assert.equal(icon.motionAmplitude,0);assert.equal(icon.textFallbackPreserved,true);assert.equal(icon.loadFailureBlocksGameplay,false);}const a=auditApexSecondaryPatternAtlas();assert.equal(a.coverage,1);assert.equal(a.uniqueCellCount,6);assert.deepEqual(a.outOfBounds,[]);assert.equal(a.passed,true);});
