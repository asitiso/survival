import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const u=new URL('../src/game/core-guard-damage-source-hysteresis-rendering.ts',import.meta.url);const game=()=>fs.readFileSync(new URL('../src/game/game.ts',import.meta.url),'utf8');async function load(){assert.equal(fs.existsSync(u),true);return import('../dist/game/core-guard-damage-source-hysteresis-rendering.js');}
test('phase 3255 core guard source hysteresis module exists',async()=>{const m=await load();assert.equal(typeof m.advanceCoreGuardDamageSourceHysteresis,'function');});
test('phase 3256 first meaningful source is adopted immediately',async()=>{const m=await load();let s=m.createCoreGuardDamageSourceHysteresisState();s=m.advanceCoreGuardDamageSourceHysteresis(s,'projectile',0);assert.equal(s.sourceClass,'projectile');});
test('phase 3257 rapid projectile-contact alternation cannot flicker the guard source every frame',async()=>{const m=await load();let s=m.advanceCoreGuardDamageSourceHysteresis(m.createCoreGuardDamageSourceHysteresisState(),'projectile',0);s=m.advanceCoreGuardDamageSourceHysteresis(s,'contact',m.CORE_GUARD_SOURCE_HOLD_SECONDS*.25);assert.equal(s.sourceClass,'projectile');});
test('phase 3258 source can hand off after the short hold expires',async()=>{const m=await load();let s=m.advanceCoreGuardDamageSourceHysteresis(m.createCoreGuardDamageSourceHysteresisState(),'projectile',0);s=m.advanceCoreGuardDamageSourceHysteresis(s,'contact',m.CORE_GUARD_SOURCE_HOLD_SECONDS*1.1);assert.equal(s.sourceClass,'contact');});
test('phase 3259 neutral sources never displace a held projectile or contact identity',async()=>{const m=await load();let s=m.advanceCoreGuardDamageSourceHysteresis(m.createCoreGuardDamageSourceHysteresisState(),'contact',0);s=m.advanceCoreGuardDamageSourceHysteresis(s,'arena',.4);assert.equal(s.sourceClass,'contact');});
test('phase 3260 live coreHit queue stores hysteresis-selected source without gameplay or snapshot mutation',async()=>{const s=game();assert.match(s,/coreGuardDamageSourceHysteresisState/);assert.match(s,/advanceCoreGuardDamageSourceHysteresis/);const a=await import('../dist/game/core-guard-damage-source-hysteresis-audit.js');const r=a.runCoreGuardDamageSourceHysteresisAudit();assert.equal(r.actionCount,9);assert.equal(r.presentationOnly,true);assert.equal(r.gameplayFormulaMutation,false);assert.equal(r.snapshotSchemaMutation,false);assert.equal(r.passed,true);});
