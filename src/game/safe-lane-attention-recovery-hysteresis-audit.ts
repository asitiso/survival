import { ACTION_BUTTONS } from './config.js';import { advanceSafeLaneAttentionRecoveryHysteresis, createSafeLaneAttentionRecoveryHysteresisState, safeLaneAttentionRecoveryPresentation } from './safe-lane-attention-recovery-hysteresis-rendering.js';
export function runSafeLaneAttentionRecoveryHysteresisAudit(){const samples:{id:string;passed:boolean}[]=[];for(const reduced of [false,true])for(let i=0;i<8;i++){let s=advanceSafeLaneAttentionRecoveryHysteresis(createSafeLaneAttentionRecoveryHysteresisState(),1,.016,reduced);for(let j=0;j<75;j++)s=advanceSafeLaneAttentionRecoveryHysteresis(s,0,1/60,reduced);const p=safeLaneAttentionRecoveryPresentation(s);samples.push({id:`${reduced}-${i}`,passed:s.level>=0&&s.level<=1&&p.recoveryAlphaScale>.8&&p.recoveryAlphaScale<=1&&p.bridgeRecoveryScale>.6&&p.bridgeRecoveryScale<=1});}return{samples,actionCount:ACTION_BUTTONS.length,presentationOnly:true as const,gameplayFormulaMutation:false as const,snapshotSchemaMutation:false as const,newAtlasCount:0,passed:samples.length===16&&samples.every(s=>s.passed)&&ACTION_BUTTONS.length===9};}
