import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const enemies=fs.readFileSync(new URL('../src/game/enemies.ts',import.meta.url),'utf8');const freeze=fs.readFileSync(new URL('../src/game/release-freeze-audit.ts',import.meta.url),'utf8');const candidate=fs.readFileSync(new URL('../src/game/release-candidate-audit.ts',import.meta.url),'utf8');const exists=fs.existsSync(new URL('../dist/game/projectile-impact-source-continuity.js',import.meta.url));const mod=exists?await import('../dist/game/projectile-impact-source-continuity.js'):null;
test('phase 2649 impact continuity backtraces from actual impact opposite actual incoming velocity',()=>{assert.ok(mod);if(!mod)return;const s=mod.projectileImpactSourceContinuity({impact:{x:400,y:300},incoming:{x:120,y:0},sourceClass:'archer',quality:'high',reducedFlash:false});assert.deepEqual(s.end,{x:400,y:300});assert.ok(s.start.x<400);assert.equal(s.start.y,300);});
test('phase 2650 zero velocity never invents a projectile source direction',()=>{assert.ok(mod);if(!mod)return;assert.equal(mod.projectileImpactSourceContinuity({impact:{x:1,y:2},incoming:{x:0,y:0},sourceClass:'archer',quality:'high',reducedFlash:false}),null);});
test('phase 2651 archer and boss impact source classes stay distinguishable without new gameplay data',()=>{assert.ok(mod);if(!mod)return;const a=mod.projectileImpactSourceContinuity({impact:{x:0,y:0},incoming:{x:2,y:3},sourceClass:'archer',quality:'high',reducedFlash:false});const b=mod.projectileImpactSourceContinuity({impact:{x:0,y:0},incoming:{x:2,y:3},sourceClass:'boss',quality:'high',reducedFlash:false});assert.notEqual(a.accent,b.accent);assert.equal(a.sourceClass,'archer');assert.equal(b.sourceClass,'boss');});
test('phase 2652 quality and reduced flash only reduce presentation length or alpha',()=>{assert.ok(mod);if(!mod)return;const x={impact:{x:100,y:100},incoming:{x:1,y:1},sourceClass:'boss',reducedFlash:false};const high=mod.projectileImpactSourceContinuity({...x,quality:'high'}),low=mod.projectileImpactSourceContinuity({...x,quality:'low'}),rf=mod.projectileImpactSourceContinuity({...x,quality:'high',reducedFlash:true});assert.ok(low.length<=high.length);assert.ok(rf.alpha<high.alpha);assert.equal(high.animated,false);assert.equal(high.motionAmplitude,0);});
test('phase 2653 impact queues preserve incoming vectors and render continuity before existing impact stamps',()=>{assert.match(enemies,/archerProjectileImpactVfx: Array<\{pos:Vec2;incoming:Vec2;ttl:number\}>/);assert.match(enemies,/bossProjectileImpactVfx: Array<\{pos:Vec2;archetype:BossArchetype;incoming:Vec2;ttl:number;maxTtl:number\}>/);assert.match(enemies,/drawProjectileImpactSourceContinuity/);assert.match(enemies,/incoming:\{\.\.\.p\.vel\}/);assert.match(enemies,/renderProjectiles\([\s\S]{0,800}presentationQuality/);});
test('phase 2654 projectile continuity audit is deterministic release-bound and presentation-only',async()=>{assert.equal(fs.existsSync(new URL('../src/game/projectile-impact-source-continuity-audit.ts',import.meta.url)),true);if(!fs.existsSync(new URL('../dist/game/projectile-impact-source-continuity-audit.js',import.meta.url)))return;const a=(await import('../dist/game/projectile-impact-source-continuity-audit.js')).runProjectileImpactSourceContinuityAudit();assert.equal(a.samples.length,64);assert.equal(a.actionCount,9);assert.equal(a.presentationOnly,true);assert.equal(a.gameplayFormulaMutation,false);assert.equal(a.snapshotSchemaMutation,false);assert.equal(a.passed,true);assert.match(freeze,/projectileImpactSourceContinuityPassed/);assert.match(candidate,/projectileImpactSourceContinuityPassed/);assert.match(candidate,/projectile-impact-source-continuity/);});
