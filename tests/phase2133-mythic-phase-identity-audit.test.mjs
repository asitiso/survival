import test from 'node:test'; import assert from 'node:assert/strict';
import { auditMythicPhaseIdentityAssets } from '../dist/game/endless/mythic-phase-identity-asset-audit.js';
test('phase 2133 audits exactly 60 deterministic Mythic Phase identity samples',()=>{const a=auditMythicPhaseIdentityAssets();assert.equal(a.samples.length,60);assert.equal(a.phaseCount,3);assert.equal(a.coverage,1);assert.equal(a.uniqueCellCount,3);assert.deepEqual(a.outOfBounds,[]);assert.equal(a.encounterToastCoverage,1);assert.equal(a.transitionToastCoverage,1);assert.equal(a.persistentRecallCoverage,1);assert.equal(a.fallbackCoverage,1);assert.equal(a.maxVisibleRecallIcons,1);assert.equal(a.textFallbackPreserved,true);assert.equal(a.imageLoadFailureNonBlocking,true);assert.equal(a.iconMotionAmplitude,0);assert.equal(a.thresholdContractMutation,false);assert.equal(a.weakpointContractMutation,false);assert.equal(a.modifierContractMutation,false);assert.equal(a.nonMythicLeak,false);assert.equal(a.actionCount,9);assert.equal(a.snapshotSchemaMutation,false);assert.deepEqual(a.issues,[]);assert.equal(a.passed,true);});
