import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs';
const url=new URL('../dist/game/endless/mythic-safe-zone-lifecycle-identity-assets.js',import.meta.url);
test('phase 2183 provides four static Mythic safe-zone lifecycle identities',async()=>{assert.equal(fs.existsSync(url),true,'safe-zone lifecycle identity module must exist');const m=await import(url.href);assert.deepEqual(m.MYTHIC_SAFE_ZONE_LIFECYCLE_IDENTITY_IDS,['stable','collapse','collapsed','reform']);assert.deepEqual(m.MYTHIC_SAFE_ZONE_LIFECYCLE_IDENTITY_ATLAS,{src:'./assets/bosses/mythic-safe-zone-lifecycle-icons.png',columns:4,rows:1,cellSize:96,width:384,height:96});for(const id of m.MYTHIC_SAFE_ZONE_LIFECYCLE_IDENTITY_IDS){const icon=m.mythicSafeZoneLifecycleIdentityIcon(id);assert.equal(icon.phase,id);assert.equal(icon.safeZoneLabelCompanion,true);assert.equal(icon.maxVisibleIcons,1);assert.equal(icon.animated,false);assert.equal(icon.motionAmplitude,0);assert.equal(icon.textFallbackPreserved,true);assert.equal(icon.loadFailureBlocksGameplay,false);}const a=m.auditMythicSafeZoneLifecycleIdentityAtlas();assert.equal(a.coverage,1);assert.equal(a.uniqueCellCount,4);assert.deepEqual(a.outOfBounds,[]);assert.equal(a.passed,true);});
