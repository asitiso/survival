# Phase 263~282 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** Add five bounded release-combat improvements covering Mythic tactic links, Last Law timeline, 12-form finisher presentation, foldable dead-space input, and raster release gating.

**Architecture:** Each feature is implemented as a focused pure module first and is then connected to the existing Game/Input/CI seam. Transient combat state lives in Game only; presentation and verification modules stay pure and deterministic.

**Tech Stack:** TypeScript, Node test runner, Canvas presentation primitives, npm scripts.

**Spec:** `docs/superpowers/specs/2026-08-30-phase263-282-release-combat-design.md`

## Global Constraints
- Combat Action count stays exactly 9.
- No new snapshot fields.
- No baseline auto-update.
- Non-foldable input behavior remains unchanged.
- New combat effects stay bounded and transient.

---

### Task 1: Mythic Tactic Attack Link
**Files:**
- Create: `src/game/endless/mythic-tactic-attack-link.ts`
- Modify: `src/game/game.ts`
- Test: `tests/mythic-tactic-attack-link.test.mjs`

**Interfaces:**
- Consumes: boss archetype, active tactic reward, current time.
- Produces: one-shot bounded attack modifier and consume helper.

- [x] Step 1: Write tests proving six archetypes create distinct non-escalating one-shot modifiers and expiry/consumption work.
- [x] Step 2: Run the targeted test and verify RED because the module does not exist.
- [x] Step 3: Implement the pure state/profile module with hard caps and no economy fields.
- [x] Step 4: Connect it at tactic reward creation and boss-special modifier consumption seams.
- [x] Step 5: Run targeted and related Mythic tests GREEN.

### Task 2: Last Law SAFE Timeline
**Files:**
- Create: `src/game/endless/last-law-safe-timeline.ts`
- Modify: `src/game/game.ts`
- Test: `tests/last-law-safe-timeline.test.mjs`

**Interfaces:**
- Consumes: SAFE telegraph timeline, Mythic HP ratio, Last Law identity.
- Produces: unified stage/label/deadline model with `autoMove:false`.

- [x] Step 1: Write tests for pre-law warning, active Last Law, normal Mythic, and bounded urgency.
- [x] Step 2: Verify RED.
- [x] Step 3: Implement the pure merger without changing Last Law activation thresholds.
- [x] Step 4: Render the unified label at the existing SAFE LANE seam.
- [x] Step 5: Run targeted and Last Law/SAFE tests GREEN.

### Task 3: Twelve Final-Form Finisher Signatures
**Files:**
- Create: `src/game/endless/final-form-finisher-signature.ts`
- Modify: `src/game/game.ts`
- Test: `tests/final-form-finisher-signature.test.mjs`

**Interfaces:**
- Consumes: `HeroFinalFormId`, existing `FinalFormFinisherFeedback`.
- Produces: presentation-only variant fields; no combat modifiers.

- [x] Step 1: Write tests covering 12 unique ids, family preservation, caps, and deterministic output.
- [x] Step 2: Verify RED.
- [x] Step 3: Implement 12 presentation signatures using compact presets.
- [x] Step 4: Apply signature geometry/label at existing finisher emission seam while retaining existing combat profile.
- [x] Step 5: Run finisher/audio integration tests GREEN.

### Task 4: Foldable Dead-Space Resolver
**Files:**
- Create: `src/game/foldable-dead-space.ts`
- Modify: `src/core/input.ts`
- Test: `tests/foldable-dead-space.test.mjs`

**Interfaces:**
- Consumes: point, safe-area profile, action layouts.
- Produces: resolved left origin/right action/neutral ownership only for foldable.

- [x] Step 1: Write tests for hinge neutrality, left lobe origin recovery, right overlap resolution, and non-foldable no-op.
- [x] Step 2: Verify RED.
- [x] Step 3: Implement deterministic resolver bounded to existing safe areas.
- [x] Step 4: Use it only inside the foldable branch of InputState.
- [x] Step 5: Run touch/input regressions GREEN.

### Task 5: Raster Release Quality Gate
**Files:**
- Create: `src/game/render-raster-release-gate.ts`
- Create: `scripts/render-release-gate.mjs`
- Modify: `package.json`
- Test: `tests/render-raster-release-gate.test.mjs`

**Interfaces:**
- Consumes: current raster CI summary/default baseline audit/action count.
- Produces: PASS/REVIEW release summary, deterministic signature, optional markdown text.

- [x] Step 1: Write tests for pass, raster review, action-count violation, deterministic markdown/signature, and no baseline mutation.
- [x] Step 2: Verify RED.
- [x] Step 3: Implement pure gate and CLI script; add `verify:release` npm command.
- [x] Step 4: Run `verify:raster` and `verify:release` GREEN.
- [x] Step 5: Add Phase 263~282 integration test, run full suite/build/diff-check, update handoff/README, commit and merge.
