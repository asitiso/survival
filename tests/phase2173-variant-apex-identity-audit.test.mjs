import test from 'node:test'; import assert from 'node:assert/strict';
import { auditVariantApexIdentityAssets } from '../dist/game/variant-apex-identity-asset-audit.js';
test('phase 2173 audits exactly 60 deterministic variant/APEX identity samples',()=>{const a=auditVariantApexIdentityAssets();assert.equal(a.samples.length,60);assert.equal(a.variantIdentityCount,6);assert.equal(a.apexIdentityCount,6);assert.equal(a.variantCoverage,1);assert.equal(a.apexCoverage,1);assert.equal(a.variantUniqueCellCount,6);assert.equal(a.apexUniqueCellCount,6);assert.equal(a.variantRecallCoverage,1);assert.equal(a.apexRecallCoverage,1);assert.equal(a.secondaryIntentCoverage,1);assert.equal(a.gameplayContractMutation,false);assert.equal(a.actionCount,9);assert.equal(a.snapshotSchemaMutation,false);assert.deepEqual(a.issues,[]);assert.equal(a.passed,true);});
