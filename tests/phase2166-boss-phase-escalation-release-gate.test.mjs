import test from 'node:test'; import assert from 'node:assert/strict';
import { auditReleaseFreeze } from '../dist/game/release-freeze-audit.js';
import { releaseCandidateAudit } from '../dist/game/release-candidate-audit.js';
test('phase 2166 release freeze binds boss phase escalation identity evidence',()=>{const f=auditReleaseFreeze();assert.equal(f.bossPhaseEscalationIdentityAssetsPassed,true);assert.equal(f.bossPhaseEscalationIdentityAssetsSamples,60);assert.equal(f.passed,true);});
test('phase 2166 candidate fails closed on forged boss phase escalation evidence and sample mutation changes signature',()=>{const base=releaseCandidateAudit();const forged=structuredClone(base.evidence);forged.releaseFreeze.bossPhaseEscalationIdentityAssetsPassed=false;forged.releaseFreeze.passed=true;const bad=releaseCandidateAudit(forged);assert.equal(bad.status,'REVIEW');assert.ok(bad.issues.includes('release-freeze'));const changed=structuredClone(base.evidence);changed.releaseFreeze.bossPhaseEscalationIdentityAssetsSamples+=1;const mutated=releaseCandidateAudit(changed);assert.notEqual(mutated.signature,base.signature);assert.match(base.markdown,/boss-phase-escalation-identity-assets safe \(60\)/);});
