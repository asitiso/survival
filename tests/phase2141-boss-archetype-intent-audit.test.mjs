import test from 'node:test'; import assert from 'node:assert/strict';
import { auditBossArchetypeIntentAssets } from '../dist/game/boss-archetype-intent-asset-audit.js';
test('phase 2141 audits exactly 60 deterministic boss archetype and intent identity samples',()=>{const a=auditBossArchetypeIntentAssets();assert.equal(a.samples.length,60);assert.equal(a.archetypeCount,6);assert.equal(a.intentCount,6);assert.equal(a.archetypeCoverage,1);assert.equal(a.intentCoverage,1);assert.equal(a.archetypeUniqueCellCount,6);assert.equal(a.intentUniqueCellCount,6);assert.equal(a.spawnToastCoverage,1);assert.equal(a.persistentRecallCoverage,1);assert.equal(a.countdownCoverage,1);assert.equal(a.specialTelegraphCoverage,1);assert.equal(a.textFallbackPreserved,true);assert.equal(a.imageLoadFailureNonBlocking,true);assert.equal(a.iconMotionAmplitude,0);assert.equal(a.patternContractMutation,false);assert.equal(a.actionCount,9);assert.equal(a.snapshotSchemaMutation,false);assert.deepEqual(a.issues,[]);assert.equal(a.passed,true);});
