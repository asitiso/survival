const clamp01 = (value) => Math.max(0, Math.min(1, Number.isFinite(value) ? value : 0));
const smooth01 = (value) => { const x = clamp01(value); return x * x * (3 - 2 * x); };
export function valueChromaReleasePresentation(input, reducedMotion = false, reducedFlash = false) { const settled = smooth01(input.reacquire), stress = clamp01(input.stress), unsettled = (1 - settled) * (.72 + stress * .18), valueScale = Math.max(input.critical ? .9 : .8, 1 - unsettled * (input.critical ? .08 : .18)), chromaScale = Math.max(input.critical ? .86 : .68, 1 - unsettled * (input.critical ? .12 : .3)), accessibility = (reducedMotion ? .999 : 1) * (reducedFlash ? .998 : 1); return { settle: settled, valueScale: Math.min(1, valueScale * accessibility), chromaScale: Math.min(1, chromaScale * accessibility), canonicalValueScale: 1, canonicalChromaScale: 1, presentationOnly: true }; }
