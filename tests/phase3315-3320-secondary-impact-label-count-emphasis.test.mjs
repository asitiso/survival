import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const u=new URL('../src/game/secondary-impact-lineage-label-count-emphasis-rendering.ts',import.meta.url);const spells=()=>fs.readFileSync(new URL('../src/game/spells.ts',import.meta.url),'utf8');async function load(){assert.equal(fs.existsSync(u),true);return import('../dist/game/secondary-impact-lineage-label-count-emphasis-rendering.js');}
test('phase 3315 secondary label count-emphasis module exists',async()=>{const m=await load();assert.equal(typeof m.secondaryImpactLineageLabelCountEmphasis,'function');});
test('phase 3316 first count render is neutral and establishes lineage memory',async()=>{const m=await load();const r=m.secondaryImpactLineageLabelCountEmphasis([], 'L1',3,true,false);assert.equal(r.presentation.scale,1);assert.equal(r.presentation.changed,false);});
test('phase 3317 count increase produces one bounded emphasis pulse',async()=>{const m=await load();let r=m.secondaryImpactLineageLabelCountEmphasis([], 'L1',3,true,false);r=m.secondaryImpactLineageLabelCountEmphasis(r.memory,'L1',5,true,false);assert.equal(r.presentation.changed,true);assert.ok(r.presentation.scale>1&&r.presentation.scale<=m.SECONDARY_IMPACT_LABEL_COUNT_MAX_SCALE);});
test('phase 3318 unchanged counts decay back toward neutral without retriggering',async()=>{const m=await load();let r=m.secondaryImpactLineageLabelCountEmphasis([], 'L1',2,true,false);r=m.secondaryImpactLineageLabelCountEmphasis(r.memory,'L1',4,true,false);const peak=r.presentation.scale;r=m.secondaryImpactLineageLabelCountEmphasis(r.memory,'L1',4,true,false);assert.equal(r.presentation.changed,false);assert.ok(r.presentation.scale<peak&&r.presentation.scale>=1);});
test('phase 3319 reduced flash compresses count emphasis and hidden labels retire memory',async()=>{const m=await load();let a=m.secondaryImpactLineageLabelCountEmphasis([], 'L1',2,true,false),b=m.secondaryImpactLineageLabelCountEmphasis([], 'L1',2,true,true);a=m.secondaryImpactLineageLabelCountEmphasis(a.memory,'L1',6,true,false);b=m.secondaryImpactLineageLabelCountEmphasis(b.memory,'L1',6,true,true);assert.ok(b.presentation.scale<a.presentation.scale);a=m.secondaryImpactLineageLabelCountEmphasis(a.memory,'L1',6,false,false);assert.equal(a.memory.some(e=>e.lineageKey==='L1'),false);});
test('phase 3320 live secondary count labels scale around settled anchors with presentation-only audit',async()=>{const s=spells();assert.match(s,/secondaryImpactLineageLabelCountEmphasis/);assert.match(s,/secondaryImpactLineageLabelCountMemory/);const a=await import('../dist/game/secondary-impact-lineage-label-count-emphasis-audit.js');const r=a.runSecondaryImpactLineageLabelCountEmphasisAudit();assert.equal(r.actionCount,9);assert.equal(r.presentationOnly,true);assert.equal(r.gameplayFormulaMutation,false);assert.equal(r.snapshotSchemaMutation,false);assert.equal(r.newAtlasCount,0);assert.equal(r.passed,true);});
