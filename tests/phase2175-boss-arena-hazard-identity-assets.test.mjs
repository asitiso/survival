import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs';
const url=new URL('../dist/game/boss-arena-hazard-identity-assets.js',import.meta.url);
test('phase 2175 provides six static arena hazard telegraph identities',async()=>{assert.equal(fs.existsSync(url),true,'boss arena hazard identity module must exist');const m=await import(url.href);assert.equal(m.BOSS_ARENA_HAZARD_IDENTITY_IDS.length,6);assert.deepEqual(m.BOSS_ARENA_HAZARD_IDENTITY_ATLAS,{src:'./assets/bosses/boss-arena-hazard-icons.png',columns:3,rows:2,cellSize:96,width:288,height:192});for(const id of m.BOSS_ARENA_HAZARD_IDENTITY_IDS){const icon=m.bossArenaHazardIdentityIcon(id);assert.equal(icon.kind,id);assert.equal(icon.primaryTelegraphIdentitySupported,true);assert.equal(icon.activeHazardIdentitySuppressed,true);assert.equal(icon.maxVisibleIcons,1);assert.equal(icon.animated,false);assert.equal(icon.motionAmplitude,0);assert.equal(icon.textFallbackPreserved,true);assert.equal(icon.loadFailureBlocksGameplay,false);}const a=m.auditBossArenaHazardIdentityAtlas();assert.equal(a.coverage,1);assert.equal(a.uniqueCellCount,6);assert.deepEqual(a.outOfBounds,[]);assert.equal(a.passed,true);});
