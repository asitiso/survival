import test from 'node:test'; import assert from 'node:assert/strict';
import { BOSS_PHASE3_ENRAGE_ATLAS,BOSS_PHASE3_ENRAGE_IDS,bossPhase3EnrageIcon,auditBossPhase3EnrageAtlas } from '../dist/game/boss-phase3-enrage-identity-assets.js';
test('phase 2160 provides six static phase III enrage identities',()=>{assert.equal(BOSS_PHASE3_ENRAGE_IDS.length,6);assert.deepEqual(BOSS_PHASE3_ENRAGE_ATLAS,{src:'./assets/bosses/boss-phase3-enrage-icons.png',columns:3,rows:2,cellSize:96,width:288,height:192});for(const id of BOSS_PHASE3_ENRAGE_IDS){const icon=bossPhase3EnrageIcon(id);assert.equal(icon.archetype,id);assert.equal(icon.phase,3);assert.equal(icon.centerCueIdentitySupported,true);assert.equal(icon.persistentRecallIdentitySupported,true);assert.equal(icon.animated,false);assert.equal(icon.motionAmplitude,0);assert.equal(icon.textFallbackPreserved,true);assert.equal(icon.loadFailureBlocksGameplay,false);}const a=auditBossPhase3EnrageAtlas();assert.equal(a.coverage,1);assert.equal(a.uniqueCellCount,6);assert.deepEqual(a.outOfBounds,[]);assert.equal(a.passed,true);});
