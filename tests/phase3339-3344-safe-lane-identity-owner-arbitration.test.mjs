import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const u=new URL('../src/game/safe-lane-identity-owner-arbitration-rendering.ts',import.meta.url);const game=()=>fs.readFileSync(new URL('../src/game/game.ts',import.meta.url),'utf8');async function load(){assert.equal(fs.existsSync(u),true);return import('../dist/game/safe-lane-identity-owner-arbitration-rendering.js');}
test('phase 3339 safe-lane identity owner arbitration module exists',async()=>{const m=await load();assert.equal(typeof m.safeLaneIdentityOwnerArbitrationPresentation,'function');});
test('phase 3340 active Last Law owns the only safe-lane identity icon',async()=>{const m=await load();const p=m.safeLaneIdentityOwnerArbitrationPresentation({lawActive:true,lawIdAvailable:true,mythic:true,directionVisible:true,attentionOwner:'law'});assert.equal(p.owner,'law');assert.equal(p.showLawIcon,true);assert.equal(p.showDirectionIcon,false);assert.equal(p.showGeometryIcon,false);});
test('phase 3341 forecast direction owns identity over passive geometry when a movement handoff is readable',async()=>{const m=await load();const p=m.safeLaneIdentityOwnerArbitrationPresentation({lawActive:false,lawIdAvailable:false,mythic:true,directionVisible:true,attentionOwner:'navigation'});assert.equal(p.owner,'direction');assert.equal(p.showDirectionIcon,true);assert.equal(p.showGeometryIcon,false);});
test('phase 3342 mythic geometry identity returns when no higher-priority direction or law cue is active',async()=>{const m=await load();const p=m.safeLaneIdentityOwnerArbitrationPresentation({lawActive:false,lawIdAvailable:false,mythic:true,directionVisible:false,attentionOwner:'navigation'});assert.equal(p.owner,'geometry');assert.equal(p.showGeometryIcon,true);});
test('phase 3343 combat attention retires secondary identity icons while keeping the safe-lane locator external to arbitration',async()=>{const m=await load();const p=m.safeLaneIdentityOwnerArbitrationPresentation({lawActive:false,lawIdAvailable:false,mythic:true,directionVisible:true,attentionOwner:'combat'});assert.equal(p.owner,'combat');assert.equal(p.showLawIcon,false);assert.equal(p.showDirectionIcon,false);assert.equal(p.showGeometryIcon,false);});
test('phase 3344 live safe-lane render gates law direction and geometry icons through one identity owner with presentation-only audit',async()=>{const s=game();assert.match(s,/safeLaneIdentityOwnerArbitrationPresentation/);assert.match(s,/safeLaneIdentity/);const a=await import('../dist/game/safe-lane-identity-owner-arbitration-audit.js');const r=a.runSafeLaneIdentityOwnerArbitrationAudit();assert.equal(r.actionCount,9);assert.equal(r.presentationOnly,true);assert.equal(r.gameplayFormulaMutation,false);assert.equal(r.snapshotSchemaMutation,false);assert.equal(r.newAtlasCount,0);assert.equal(r.passed,true);});
