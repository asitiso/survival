import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const u=new URL('../src/game/secondary-impact-lineage-label-connector-rendering.ts',import.meta.url);const spells=()=>fs.readFileSync(new URL('../src/game/spells.ts',import.meta.url),'utf8');async function load(){assert.equal(fs.existsSync(u),true);return import('../dist/game/secondary-impact-lineage-label-connector-rendering.js');}
test('phase 3369 secondary lineage label connector module exists',async()=>{const m=await load();assert.equal(typeof m.secondaryImpactLineageLabelConnectorPresentation,'function');});
test('phase 3370 a settled unshifted label does not add an unnecessary connector',async()=>{const m=await load();const p=m.secondaryImpactLineageLabelConnectorPresentation({anchor:{x:200,y:200},labelPos:{x:200,y:170},visible:true,biasApplied:false,settled:true},false);assert.equal(p.visible,false);});
test('phase 3371 an inward-biased edge label gets one short ownership connector back toward its lineage anchor',async()=>{const m=await load();const p=m.secondaryImpactLineageLabelConnectorPresentation({anchor:{x:20,y:250},labelPos:{x:48,y:216},visible:true,biasApplied:true,settled:true},false);assert.equal(p.visible,true);assert.ok(Math.hypot(p.to.x-p.from.x,p.to.y-p.from.y)<=m.SECONDARY_IMPACT_LABEL_CONNECTOR_MAX_LENGTH+1e-9);assert.ok(p.alpha>0);});
test('phase 3372 a still-moving label can retain a weaker connector even without edge bias',async()=>{const m=await load();const p=m.secondaryImpactLineageLabelConnectorPresentation({anchor:{x:400,y:300},labelPos:{x:438,y:270},visible:true,biasApplied:false,settled:false},false);assert.equal(p.visible,true);assert.ok(p.alpha<.34);});
test('phase 3373 reduced flash lowers connector emphasis without changing endpoints or visibility',async()=>{const m=await load();const input={anchor:{x:20,y:250},labelPos:{x:48,y:216},visible:true,biasApplied:true,settled:true},a=m.secondaryImpactLineageLabelConnectorPresentation(input,false),b=m.secondaryImpactLineageLabelConnectorPresentation(input,true);assert.equal(b.visible,a.visible);assert.deepEqual(b.from,a.from);assert.deepEqual(b.to,a.to);assert.ok(b.alpha<a.alpha);});
test('phase 3374 SpellSystem draws connector ownership before count text with presentation-only audit',async()=>{const s=spells();assert.match(s,/secondaryImpactLineageLabelConnectorPresentation/);assert.match(s,/connector\.visible/);assert.ok(s.indexOf('connector.visible')<s.lastIndexOf('ctx.fillText(label.text'));const a=await import('../dist/game/secondary-impact-lineage-label-connector-audit.js');const r=a.runSecondaryImpactLineageLabelConnectorAudit();assert.equal(r.actionCount,9);assert.equal(r.presentationOnly,true);assert.equal(r.gameplayFormulaMutation,false);assert.equal(r.snapshotSchemaMutation,false);assert.equal(r.newAtlasCount,0);assert.equal(r.passed,true);});
