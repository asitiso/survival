import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const u=new URL('../src/game/secondary-impact-lineage-label-connector-capacity-budget-rendering.ts',import.meta.url);const spells=()=>fs.readFileSync(new URL('../src/game/spells.ts',import.meta.url),'utf8');async function load(){assert.equal(fs.existsSync(u),true);return import('../dist/game/secondary-impact-lineage-label-connector-capacity-budget-rendering.js');}
test('phase 3387 secondary lineage connector capacity module exists',async()=>{const m=await load();assert.equal(typeof m.secondaryImpactLineageLabelConnectorCapacityBudget,'function');});
test('phase 3388 high quality allows at most two connector lines despite four visible count labels',async()=>{const m=await load();assert.equal(m.secondaryImpactLineageLabelConnectorCapacityBudget('high').maxVisible,2);});
test('phase 3389 medium keeps two connectors while low quality keeps only the highest-priority one',async()=>{const m=await load();assert.equal(m.secondaryImpactLineageLabelConnectorCapacityBudget('medium').maxVisible,2);assert.equal(m.secondaryImpactLineageLabelConnectorCapacityBudget('low').maxVisible,1);});
test('phase 3390 connector capacity never changes the independent count-label capacity contract',async()=>{const m=await load();for(const q of ['high','medium','low']){const r=m.secondaryImpactLineageLabelConnectorCapacityBudget(q);assert.equal(r.hidesLabels,false);assert.ok(r.maxVisible>=1);}});
test('phase 3391 capacity budget is deterministic and contains no timing or gameplay state',async()=>{const m=await load();for(const q of ['high','medium','low'])assert.deepEqual(m.secondaryImpactLineageLabelConnectorCapacityBudget(q),m.secondaryImpactLineageLabelConnectorCapacityBudget(q));});
test('phase 3392 SpellSystem consumes connector capacity inside existing priority order with presentation-only audit',async()=>{const s=spells();assert.match(s,/secondaryImpactLineageLabelConnectorCapacityBudget/);assert.match(s,/secondaryConnectorCount/);assert.match(s,/secondaryConnectorCapacity\.maxVisible/);const a=await import('../dist/game/secondary-impact-lineage-label-connector-capacity-budget-audit.js');const r=a.runSecondaryImpactLineageLabelConnectorCapacityBudgetAudit();assert.equal(r.actionCount,9);assert.equal(r.presentationOnly,true);assert.equal(r.gameplayFormulaMutation,false);assert.equal(r.snapshotSchemaMutation,false);assert.equal(r.newAtlasCount,0);assert.equal(r.passed,true);});
