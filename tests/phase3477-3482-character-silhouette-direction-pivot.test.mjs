import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';async function load(){return import('../dist/game/character-silhouette-direction-pivot-rendering.js');}const source=()=>fs.readFileSync(new URL('../src/game/enemies.ts',import.meta.url),'utf8');
const owner=(name,x,y)=>({owner:name,facingX:x,facingY:y,trailDistanceScale:1,presentationOnly:true});
test('phase 3477 specialist attack silhouette pivots toward target without a one-frame 180 degree snap',async()=>{const m=await load();const p=m.characterSilhouetteDirectionPivotPresentation({locomotion:{x:1,y:0},owned:owner('attack',-1,0),turn:1},false);assert.ok(p.facingX>-1&&p.facingY!==0);});
test('phase 3478 hit ownership pivots more decisively than ordinary recovery ownership',async()=>{const m=await load();const hit=m.characterSilhouetteDirectionPivotPresentation({locomotion:{x:1,y:0},owned:owner('hit',0,1),turn:.4},false),rec=m.characterSilhouetteDirectionPivotPresentation({locomotion:{x:1,y:0},owned:owner('recovery',0,1),turn:.4},false);assert.ok(hit.facingY>rec.facingY);});
test('phase 3479 boss special direction remains target aligned while keeping a bounded trail pivot',async()=>{const m=await load();const p=m.characterSilhouetteDirectionPivotPresentation({locomotion:{x:.7,y:.7},owned:owner('special',-1,0),turn:.6},false);assert.equal(p.owner,'special');assert.ok(p.pivotWeight>.5&&p.pivotWeight<1);});
test('phase 3480 locomotion owner never invents a secondary pivot',async()=>{const m=await load();const p=m.characterSilhouetteDirectionPivotPresentation({locomotion:{x:0,y:1},owned:owner('locomotion',0,1),turn:1},false);assert.equal(p.pivotWeight,0);assert.ok(p.facingY>.99);});
test('phase 3481 reduced motion adopts owned silhouette direction directly',async()=>{const m=await load();const p=m.characterSilhouetteDirectionPivotPresentation({locomotion:{x:1,y:0},owned:owner('attack',0,1),turn:1},true);assert.equal(p.pivotWeight,1);assert.ok(p.facingY>.99);});
test('phase 3482 live specialist and boss silhouette translations use pivoted owned facing',async()=>{const s=source();assert.match(s,/characterSilhouetteDirectionPivotPresentation/);assert.match(s,/silhouettePivot\.facingX/);assert.match(s,/silhouettePivot\.facingY/);const a=await import('../dist/game/character-silhouette-direction-pivot-audit.js');const r=a.runCharacterSilhouetteDirectionPivotAudit();assert.equal(r.actionCount,9);assert.equal(r.presentationOnly,true);assert.equal(r.gameplayFormulaMutation,false);assert.equal(r.newAtlasCount,0);assert.equal(r.passed,true);});
