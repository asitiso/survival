import test from 'node:test';import assert from 'node:assert/strict';
import { auditHeroCombatVisualIdentityAssets } from '../dist/game/hero-combat-visual-identity-asset-audit.js';
test('phase 2098-2099 audits exactly sixty deterministic hero combat visual identity samples at the existing verification volume',()=>{const a=auditHeroCombatVisualIdentityAssets();assert.equal(a.samples.length,60);assert.equal(a.heroMeterCount,4);assert.equal(a.heroMeterCoverage,1);assert.equal(a.heroMeterUniqueCellCount,4);assert.equal(a.arcaneComboCount,4);assert.equal(a.arcaneComboCoverage,1);assert.equal(a.arcaneComboUniqueCellCount,4);assert.equal(a.tacticalToastCoverage,1);assert.equal(a.fusionToastCoverage,1);assert.equal(a.textFallbackPreserved,true);assert.equal(a.imageLoadFailureNonBlocking,true);assert.equal(a.iconMotionAmplitude,0);assert.equal(a.heroMeterContractMutation,false);assert.equal(a.comboContractMutation,false);assert.equal(a.tacticalContractMutation,false);assert.equal(a.fusionContractMutation,false);assert.equal(a.actionCount,9);assert.equal(a.snapshotSchemaMutation,false);assert.deepEqual(a.issues,[]);assert.equal(a.passed,true);});
