import test from 'node:test'; import assert from 'node:assert/strict';
import { auditReleaseFreeze } from '../dist/game/release-freeze-audit.js';
import { releaseCandidateAudit } from '../dist/game/release-candidate-audit.js';
test('phase 2214 release freeze binds run mission pace reward identity evidence',()=>{const f=auditReleaseFreeze();assert.equal(f.runMissionPaceRewardIdentityAssetsPassed,true);assert.equal(f.runMissionPaceRewardIdentityAssetsSamples,60);assert.equal(f.passed,true);});
test('phase 2214 candidate fails closed on forged mission evidence and sample mutation changes signature',()=>{const base=releaseCandidateAudit();const forged=structuredClone(base.evidence);forged.releaseFreeze.runMissionPaceRewardIdentityAssetsPassed=false;forged.releaseFreeze.passed=true;const bad=releaseCandidateAudit(forged);assert.equal(bad.status,'REVIEW');assert.ok(bad.issues.includes('release-freeze'));const changed=structuredClone(base.evidence);changed.releaseFreeze.runMissionPaceRewardIdentityAssetsSamples+=1;const mutated=releaseCandidateAudit(changed);assert.notEqual(mutated.signature,base.signature);assert.match(base.markdown,/run-mission-pace-reward-identity-assets safe \(60\)/);});
