# Phase 83-102 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrate progression-valid Build Capsule replay, distinct Mythic Last Laws, Final Form attack pulses, checkpoint UX, and mobile input/render polish.

**Architecture:** New pure modules live under `src/game/endless/` or `src/domain/`; `game.ts` only bridges existing runtime effects, snapshots, results, and combat APIs. Input improvements are pure helpers consumed by `InputState`.

**Tech Stack:** TypeScript, browser Canvas/Pointer Events, Node test runner.

**Spec:** `docs/superpowers/specs/2026-08-30-phase83-102-design.md`

## Global Constraints
- Nine combat ActionIds remain unchanged.
- Replay starts from encoded start conditions but does not grant final loadout.
- Snapshot remains compact and migration-safe.
- No new mandatory modal flow.
- VFX/presentation degrades before enemy logic.

---

### Task 1: Build Capsule Replay
**Files:** Create `src/domain/build-replay.ts`; modify `src/domain/run-snapshot.ts`, `src/game/game.ts`; test `tests/build-replay.test.mjs`.

- [ ] Write failing tests for valid/invalid capsule replay plans, deterministic progress scoring, and snapshot replay-capsule sanitation.
- [ ] Run the targeted tests and confirm missing-module/field failures.
- [ ] Implement replay plan construction, progress scoring, optional `replayCapsule` snapshot field, and Game replay wiring.
- [ ] Run targeted tests and build.
- [ ] Commit.

### Task 2: Mythic Last Law Identity
**Files:** Create `src/game/endless/mythic-last-law-identity.ts`; modify `src/game/game.ts`; test `tests/endless-mythic-last-law-identity.test.mjs`.

- [ ] Write failing tests for six distinct identities, weakpoint relief and hard caps.
- [ ] Run RED.
- [ ] Implement bounded identity modifiers layered over the existing Last Law profile.
- [ ] Wire identity label/modifiers into boss encounter calculation and toast.
- [ ] Run targeted tests and commit.

### Task 3: Final Form Attack Patterns
**Files:** Create `src/game/endless/final-form-patterns.ts`; modify `src/game/game.ts`; test `tests/endless-final-form-patterns.test.mjs`.

- [ ] Write failing tests proving all 12 forms map to bounded pulse specs with at least four distinct mechanical shapes.
- [ ] Run RED.
- [ ] Implement pulse specs and one-shot Game combat application on signature activation.
- [ ] Verify tests/build and commit.

### Task 4: Mid-run Checkpoint UX
**Files:** Create `src/game/endless/run-checkpoints.ts`; modify `src/game/endless/types.ts`, `runtime.ts`, `snapshot.ts`, `src/game/game.ts`; test `tests/endless-run-checkpoints.test.mjs`.

- [ ] Write failing tests for milestone scheduling, exactly-once behavior, snapshot migration, and effect emission.
- [ ] Run RED.
- [ ] Implement compact checkpoint state and runtime effect.
- [ ] Wire effect to forced snapshot+journal append and non-pausing toast.
- [ ] Verify and commit.

### Task 5: Mobile Touch & Presentation Budget
**Files:** Create `src/core/touch-controls.ts`; modify `src/core/input.ts`, `src/game/endless/mobile-frame-governor.ts`; test `tests/mobile-touch-polish.test.mjs` and existing governor tests.

- [ ] Write failing tests for nearest normalized hit selection, joystick deadzone/remap, and tier presentation budgets.
- [ ] Run RED.
- [ ] Implement helpers and connect them to InputState/governor policy.
- [ ] Verify and commit.

### Task 6: Integration, Docs, Release Artifact
**Files:** Modify `tests/endless-phase83-102-integration.test.mjs`, `README.md`; create `docs/PHASE83-102-HANDOFF.md`.

- [ ] Add integration assertions for imports/wiring, replay persistence, checkpoint effect, and unchanged ActionId count.
- [ ] Run full test suite.
- [ ] Run `npm run build` and `git diff --check`.
- [ ] Update handoff/README and commit.
- [ ] Merge verified branch to `main`, re-run full verification, HTTP smoke test and archive integrity check.
