name: Phase 4545-4550 Dense Affix Arbitration RED

on:
  push:
    paths:
      - '.sync/phase4496.ready'

permissions:
  contents: read

jobs:
  red:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work/phase4533-4550-elite-multiaffix-arbitration
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Phase 4545-4550 RED
        run: node --test tests/phase4545-4550-elite-affix-dense-arbitration.test.mjs
