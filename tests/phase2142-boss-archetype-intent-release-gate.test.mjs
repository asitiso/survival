import test from 'node:test'; import assert from 'node:assert/strict';
import { auditReleaseFreeze } from '../dist/game/release-freeze-audit.js';
import { releaseCandidateAudit } from '../dist/game/release-candidate-audit.js';
test('phase 2142 release freeze binds boss archetype intent identity evidence',()=>{const f=auditReleaseFreeze();assert.equal(f.bossArchetypeIntentIdentityAssetsPassed,true);assert.equal(f.bossArchetypeIntentIdentityAssetsSamples,60);assert.equal(f.passed,true);});
test('phase 2142 candidate fails closed on forged boss archetype intent evidence and sample mutation changes signature',()=>{const base=releaseCandidateAudit();const forged=structuredClone(base.evidence);forged.releaseFreeze.bossArchetypeIntentIdentityAssetsPassed=false;forged.releaseFreeze.passed=true;const bad=releaseCandidateAudit(forged);assert.equal(bad.status,'REVIEW');assert.ok(bad.issues.includes('release-freeze'));const changed=structuredClone(base.evidence);changed.releaseFreeze.bossArchetypeIntentIdentityAssetsSamples+=1;const mutated=releaseCandidateAudit(changed);assert.notEqual(mutated.signature,base.signature);assert.match(base.markdown,/boss-archetype-intent-identity-assets safe \(60\)/);});
