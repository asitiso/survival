# Phase 123-142 Combat Feel & Mobile Finish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Finish boss arena identity, Final Form flow, long-run comfort, opening pacing, and landscape HUD priority without adding controls.

**Architecture:** Five focused modules expose bounded profiles/modifiers. `game.ts` composes them at existing update/render seams; `BossArenaSystem` consumes geometry through an optional context field. Final Form flow is transient and resets with the run.

**Tech Stack:** TypeScript, Canvas 2D, Node test runner.

**Spec:** `docs/superpowers/specs/2026-08-30-phase123-142-design.md`

## Global Constraints
- Nine combat actions remain unchanged.
- No new modal/menu for these phases.
- Danger telegraphs remain visible at every presentation tier.
- Opening pacing applies only before 600 seconds.

---

### Task 1: Boss Arena Geometry
**Files:** create `src/game/endless/mythic-arena-geometry.ts`; modify `src/game/boss-arena.ts`, `src/game/game.ts`; test `tests/endless-mythic-arena-geometry.test.mjs`.
- [ ] Write failing geometry identity tests.
- [ ] Run targeted test and confirm missing module failure.
- [ ] Implement bounded geometry profiles and weakpoint relief.
- [ ] Integrate optional geometry into hazard placement/rendering.
- [ ] Run targeted + existing boss arena tests.

### Task 2: Final Form Flow Combo
**Files:** create `src/game/endless/final-form-flow.ts`; modify `src/game/game.ts`; test `tests/endless-final-form-flow.test.mjs`.
- [ ] Write failing streak/expiry/family tests.
- [ ] Run RED.
- [ ] Implement transient flow state and bounded modifiers.
- [ ] Hook movement and successful casts into the existing game loop.
- [ ] Run targeted + Final Form regression tests.

### Task 3: Long-Run Fatigue Relief
**Files:** create `src/game/endless/long-run-comfort.ts`; modify `src/game/game.ts`; test `tests/endless-long-run-comfort.test.mjs`.
- [ ] Write failing 2h/4h/8h presentation policy tests.
- [ ] Run RED.
- [ ] Implement non-difficulty presentation policy.
- [ ] Apply HUD label/VFX density limits only.
- [ ] Run targeted tests.

### Task 4: First Ten Minute Combat Pacing
**Files:** create `src/game/opening-pacing.ts`; modify `src/game/game.ts`; test `tests/opening-pacing.test.mjs`.
- [ ] Write failing band and cap tests.
- [ ] Run RED.
- [ ] Implement bounded pacing profile.
- [ ] Compose with existing spawn/elite/reward multipliers before ten minutes only.
- [ ] Run targeted + tuning tests.

### Task 5: Landscape HUD Priority
**Files:** modify `src/game/landscape-hud.ts`, `src/game/game.ts`; test `tests/landscape-hud-priority.test.mjs`.
- [ ] Write failing critical-priority/label-limit tests.
- [ ] Run RED.
- [ ] Implement priority selector.
- [ ] Integrate boss/mythic and long-run comfort context.
- [ ] Run targeted + landscape input regressions.

### Task 6: Integration & Verification
**Files:** create `tests/phase123-142-integration.test.mjs`; update README/handoff.
- [ ] Assert 9 ActionIds and no extra result controls.
- [ ] Build and run targeted integration tests.
- [ ] Run full `npm test`.
- [ ] Run `git diff --check`.
- [ ] Commit, verify committed branch, fast-forward `main`, verify merged main, HTTP smoke, archive ZIP.
