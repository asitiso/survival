import test from 'node:test'; import assert from 'node:assert/strict';
import { BOSS_PHASE2_ESCALATION_ATLAS,BOSS_PHASE2_ESCALATION_IDS,bossPhase2EscalationIcon,auditBossPhase2EscalationAtlas } from '../dist/game/boss-phase2-escalation-identity-assets.js';
test('phase 2159 provides six static phase II escalation identities',()=>{assert.equal(BOSS_PHASE2_ESCALATION_IDS.length,6);assert.deepEqual(BOSS_PHASE2_ESCALATION_ATLAS,{src:'./assets/bosses/boss-phase2-escalation-icons.png',columns:3,rows:2,cellSize:96,width:288,height:192});for(const id of BOSS_PHASE2_ESCALATION_IDS){const icon=bossPhase2EscalationIcon(id);assert.equal(icon.archetype,id);assert.equal(icon.phase,2);assert.equal(icon.centerCueIdentitySupported,true);assert.equal(icon.persistentRecallIdentitySupported,true);assert.equal(icon.animated,false);assert.equal(icon.motionAmplitude,0);assert.equal(icon.textFallbackPreserved,true);assert.equal(icon.loadFailureBlocksGameplay,false);}const a=auditBossPhase2EscalationAtlas();assert.equal(a.coverage,1);assert.equal(a.uniqueCellCount,6);assert.deepEqual(a.outOfBounds,[]);assert.equal(a.passed,true);});
