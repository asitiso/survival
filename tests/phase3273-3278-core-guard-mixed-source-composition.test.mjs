import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const u=new URL('../src/game/core-guard-mixed-source-composition-rendering.ts',import.meta.url);const game=()=>fs.readFileSync(new URL('../src/game/game.ts',import.meta.url),'utf8');async function load(){assert.equal(fs.existsSync(u),true);return import('../dist/game/core-guard-mixed-source-composition-rendering.js');}
test('phase 3273 core guard mixed-source composition module exists',async()=>{const m=await load();assert.equal(typeof m.coreGuardMixedSourceCompositionPresentation,'function');});
test('phase 3274 rapid mixed pressure exposes both source accents without a second damage owner',async()=>{const m=await load();const p=m.coreGuardMixedSourceCompositionPresentation({sourceClass:'projectile',mixedPressure:true,owner:'world-guard',ttl:.28,maxTtl:.34},false);assert.equal(p.mixed,true);assert.ok(p.projectileAccentAlpha>0);assert.ok(p.contactAccentAlpha>0);assert.equal(p.duplicatesDamageCue,false);});
test('phase 3275 single-source pressure keeps only its own accent family',async()=>{const m=await load();const p=m.coreGuardMixedSourceCompositionPresentation({sourceClass:'contact',mixedPressure:false,owner:'shared',ttl:.28,maxTtl:.34},false);assert.equal(p.mixed,false);assert.equal(p.projectileAccentAlpha,0);assert.ok(p.contactAccentAlpha>0);});
test('phase 3276 mixed composition uses balanced body geometry rather than flickering between extremes',async()=>{const m=await load();const p=m.coreGuardMixedSourceCompositionPresentation({sourceClass:'projectile',mixedPressure:true,owner:'world-guard',ttl:.28,maxTtl:.34},false);assert.ok(Math.abs(p.bodyScaleX-p.bodyScaleY)<.16);});
test('phase 3277 reduced flash only lowers additive mixed accents',async()=>{const m=await load();const input={sourceClass:'projectile',mixedPressure:true,owner:'world-guard',ttl:.28,maxTtl:.34};const a=m.coreGuardMixedSourceCompositionPresentation(input,false),b=m.coreGuardMixedSourceCompositionPresentation(input,true);assert.equal(a.bodyScaleX,b.bodyScaleX);assert.ok(b.projectileAccentAlpha<=a.projectileAccentAlpha);assert.ok(b.contactAccentAlpha<=a.contactAccentAlpha);});
test('phase 3278 live hysteresis records mixed pressure and survival render consumes mixed composition presentation-only',async()=>{const s=game();assert.match(s,/mixedPressure/);assert.match(s,/coreGuardMixedSourceCompositionPresentation/);const h=await import('../dist/game/core-guard-damage-source-hysteresis-rendering.js');let st=h.advanceCoreGuardDamageSourceHysteresis(h.createCoreGuardDamageSourceHysteresisState(),'projectile',0);st=h.advanceCoreGuardDamageSourceHysteresis(st,'contact',.02);assert.equal(st.mixedPressure,true);const a=await import('../dist/game/core-guard-mixed-source-composition-audit.js');const r=a.runCoreGuardMixedSourceCompositionAudit();assert.equal(r.actionCount,9);assert.equal(r.presentationOnly,true);assert.equal(r.gameplayFormulaMutation,false);assert.equal(r.snapshotSchemaMutation,false);assert.equal(r.passed,true);});
