import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs';
const url=new URL('../dist/game/objective-action-identity-assets.js',import.meta.url);
test('phase 2191 provides three static tactical objective action identities',async()=>{assert.equal(fs.existsSync(url),true,'objective action identity module must exist');const m=await import(url.href);assert.deepEqual(m.OBJECTIVE_ACTION_IDENTITY_IDS,['seal','defend','endure']);assert.deepEqual(m.OBJECTIVE_ACTION_IDENTITY_ATLAS,{src:'./assets/ui/objective-action-icons.png',columns:3,rows:1,cellSize:96,width:288,height:96});assert.equal(m.objectiveActionIdentityForObjective('riftSeal'),'seal');assert.equal(m.objectiveActionIdentityForObjective('beaconDefense'),'defend');assert.equal(m.objectiveActionIdentityForObjective('cursedAltar'),'endure');for(const id of m.OBJECTIVE_ACTION_IDENTITY_IDS){const icon=m.objectiveActionIdentityIcon(id);assert.equal(icon.id,id);assert.equal(icon.maxVisibleIcons,1);assert.equal(icon.animated,false);assert.equal(icon.motionAmplitude,0);assert.equal(icon.loadFailureBlocksGameplay,false);}const a=m.auditObjectiveActionIdentityAtlas();assert.equal(a.coverage,1);assert.equal(a.uniqueCellCount,3);assert.deepEqual(a.outOfBounds,[]);assert.equal(a.passed,true);});
