const clamp01=(value:number)=>Math.max(0,Math.min(1,Number.isFinite(value)?value:0));
type GlowFamily='projectile'|'impact'|'hazard'|'safeLane'|'specialist'|'spell';
export function glowOwnershipPresentation(input:{family:GlowFamily;crowd:number;critical:boolean;bossProtected:boolean;safeLaneVisible:boolean},reducedMotion=false,reducedFlash=false){const crowd=clamp01(input.crowd),protectedGlow=input.family==='safeLane'||input.bossProtected||(input.family==='projectile'&&input.critical),floor=protectedGlow?1:input.critical?.92:input.family==='projectile'?.8:input.family==='spell'?.72:input.family==='specialist'?.7:input.family==='hazard'?.68:.62,accessibility=protectedGlow?1:(reducedMotion?.995:1)*(reducedFlash?.96:1);return{canonicalGlowBlurScale:1,glowBlurScale:protectedGlow?1:Math.max(floor,1-crowd*(1-floor))*accessibility,protectedGlow,presentationOnly:true as const};}
export function glowOwnershipBudgetPresentation(input:{criticalCount:number;crowd:number;bossActive:boolean;safeLaneVisible:boolean},reducedMotion=false,reducedFlash=false){const critical=clamp01(Math.max(0,input.criticalCount)/3),crowd=clamp01(input.crowd),stress=clamp01(crowd*.78+critical*.18+(input.bossActive?.04:0)),laneReserve=input.safeLaneVisible?.02:0,accessibility=(reducedMotion?.99:1)*(reducedFlash?.94:1);return{stress,canonicalGlowBlurScale:1,secondaryGlowBlurScale:Math.max(.72,1-stress*.22-laneReserve)*accessibility,safeLaneGlowBlurScale:1,presentationOnly:true as const};}
