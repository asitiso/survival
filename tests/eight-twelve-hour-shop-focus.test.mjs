import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import { eightTwelveHourShopFocus } from '../dist/game/eight-twelve-hour-shop-focus.js';
const complete={coins:9999,weapon:{id:'arcane-staff',kind:'weapon',name:'Arcane',rank:5,power:.15,legendary:true},armor:{id:'iron-robe',kind:'armor',name:'Robe',rank:5,power:.08,legendary:true},healingPotions:3};
test('phase 683 8-12h completed stocked builds put the shop into a low-attention dormant state without disabling it',()=>{const p=eightTwelveHourShopFocus(8*3600+1,complete);assert.equal(p.active,true);assert.equal(p.dormant,true);assert.equal(p.keepClickable,true);assert.equal(p.showTokenCount,false);assert.ok(p.visualAlpha<=.5);});
test('phase 684 incomplete gear or low potions immediately restores visible shop need',()=>{for(const s of [{...complete,weapon:{...complete.weapon,rank:4}},{...complete,healingPotions:1}]){const p=eightTwelveHourShopFocus(10*3600,s);assert.equal(p.dormant,false);assert.equal(p.showTokenCount,true);assert.equal(p.visualAlpha,1);}});
test('phase 685 pre-eight-hour runs retain the existing 4-8h shop presentation contract',()=>{const p=eightTwelveHourShopFocus(8*3600-1,complete);assert.equal(p.active,false);assert.equal(p.dormant,false);assert.equal(p.visualAlpha,1);});
test('phase 686 deepest shop quieting is presentation-only and removes no control or economy behavior',()=>{const p=eightTwelveHourShopFocus(12*3600,complete);assert.equal(p.economyMutation,false);assert.equal(p.newControlCount,0);assert.equal(p.keepClickable,true);assert.ok(p.estimatedAttentionReduction>=.7);const source=fs.readFileSync(new URL('../src/game/game.ts',import.meta.url),'utf8');assert.match(source,/eightTwelveHourShopFocus/);});
