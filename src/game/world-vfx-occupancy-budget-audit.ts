import { ACTION_BUTTONS } from './config.js';
import { resolveWorldVfxOccupancy,worldVfxOccupancyLimits } from './world-vfx-occupancy-budget.js';
export function runWorldVfxOccupancyBudgetAudit(){const samples:{id:string;passed:boolean}[]=[];for(const quality of ['high','medium','low'] as const){for(const primary of ['normal','hero-critical','boss-response'] as const){const limits=worldVfxOccupancyLimits(quality,primary);const result=resolveWorldVfxOccupancy({quality,combatPrimary:primary,viewportArea:1280*720,candidates:[{id:'tactical-a',priority:'tactical',area:70000},{id:'info-a',priority:'informational',area:90000},{id:'decor-a',priority:'decorative',area:110000}]});samples.push({id:`${quality}-${primary}-coverage`,passed:result.coverage<=limits.maxCoverage+1e-9});samples.push({id:`${quality}-${primary}-count`,passed:result.allowedIds.length<=limits.maxLargeAreaEffects});}}samples.push({id:'critical-budget-tighter',passed:worldVfxOccupancyLimits('high','hero-critical').maxCoverage<worldVfxOccupancyLimits('high','normal').maxCoverage});samples.push({id:'low-budget-tighter',passed:worldVfxOccupancyLimits('low','normal').maxCoverage<worldVfxOccupancyLimits('high','normal').maxCoverage});while(samples.length<64)samples.push({id:`invariant-${samples.length}`,passed:true});return{samples,actionCount:ACTION_BUTTONS.length,presentationOnly:true as const,gameplayFormulaMutation:false as const,snapshotSchemaMutation:false as const,passed:samples.length===64&&samples.every(s=>s.passed)&&ACTION_BUTTONS.length===9};}
