import test from 'node:test'; import assert from 'node:assert/strict';
import { auditWeakpointBenefitIdentityAssets } from '../dist/game/weakpoint-benefit-identity-asset-audit.js';
test('phase 2157 audits exactly 60 deterministic weakpoint break and benefit identity samples',()=>{const a=auditWeakpointBenefitIdentityAssets();assert.equal(a.samples.length,60);assert.equal(a.breakIdentityCount,6);assert.equal(a.benefitIdentityCount,6);assert.equal(a.breakCoverage,1);assert.equal(a.benefitCoverage,1);assert.equal(a.breakUniqueCellCount,6);assert.equal(a.benefitUniqueCellCount,6);assert.equal(a.completionToastCoverage,1);assert.equal(a.persistentRecallCoverage,1);assert.equal(a.infernoExpiryCoverage,1);assert.equal(a.iconMotionAmplitude,0);assert.equal(a.gameplayContractMutation,false);assert.equal(a.actionCount,9);assert.equal(a.snapshotSchemaMutation,false);assert.deepEqual(a.issues,[]);assert.equal(a.passed,true);});
