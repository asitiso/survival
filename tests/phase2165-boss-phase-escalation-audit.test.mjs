import test from 'node:test'; import assert from 'node:assert/strict';
import { auditBossPhaseEscalationIdentityAssets } from '../dist/game/boss-phase-escalation-identity-asset-audit.js';
test('phase 2165 audits exactly 60 deterministic boss phase escalation identity samples',()=>{const a=auditBossPhaseEscalationIdentityAssets();assert.equal(a.samples.length,60);assert.equal(a.phase2IdentityCount,6);assert.equal(a.phase3IdentityCount,6);assert.equal(a.phase2Coverage,1);assert.equal(a.phase3Coverage,1);assert.equal(a.phase2UniqueCellCount,6);assert.equal(a.phase3UniqueCellCount,6);assert.equal(a.centerCueCoverage,1);assert.equal(a.persistentRecallCoverage,1);assert.equal(a.gameplayContractMutation,false);assert.equal(a.actionCount,9);assert.equal(a.snapshotSchemaMutation,false);assert.deepEqual(a.issues,[]);assert.equal(a.passed,true);});
