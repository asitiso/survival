import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs';
const url=new URL('../dist/game/field-event-effect-profile-identity-assets.js',import.meta.url);
test('phase 2200 provides five static field-event effect profile identities',async()=>{assert.equal(fs.existsSync(url),true,'field-event effect profile identity module must exist');const m=await import(url.href);assert.deepEqual(m.FIELD_EVENT_EFFECT_PROFILE_IDENTITY_IDS,['gold-bounty','free-supply','mana-tradeoff','gold-elite-tradeoff','elite-pressure']);assert.deepEqual(m.FIELD_EVENT_EFFECT_PROFILE_IDENTITY_ATLAS,{src:'./assets/ui/field-event-effect-profile-icons.png',columns:5,rows:1,cellSize:96,width:480,height:96});assert.equal(m.fieldEventEffectProfileIdentityForEvent('goldenGoblin'),'gold-bounty');assert.equal(m.fieldEventEffectProfileIdentityForEvent('supplyDrop'),'free-supply');assert.equal(m.fieldEventEffectProfileIdentityForEvent('manaStorm'),'mana-tradeoff');assert.equal(m.fieldEventEffectProfileIdentityForEvent('goldenNight'),'gold-elite-tradeoff');assert.equal(m.fieldEventEffectProfileIdentityForEvent('eliteRush'),'elite-pressure');for(const id of m.FIELD_EVENT_EFFECT_PROFILE_IDENTITY_IDS){const icon=m.fieldEventEffectProfileIdentityIcon(id);assert.equal(icon.id,id);assert.equal(icon.maxVisibleIcons,1);assert.equal(icon.animated,false);assert.equal(icon.motionAmplitude,0);assert.equal(icon.textFallbackPreserved,true);assert.equal(icon.loadFailureBlocksGameplay,false);}const a=m.auditFieldEventEffectProfileIdentityAtlas();assert.equal(a.coverage,1);assert.equal(a.uniqueCellCount,5);assert.deepEqual(a.outOfBounds,[]);assert.equal(a.passed,true);});
