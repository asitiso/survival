import test from 'node:test';import assert from 'node:assert/strict';
import { auditSpellEvolutionIdentityAssets } from '../dist/game/spell-evolution-identity-asset-audit.js';
test('phase 2117 audits exactly sixty deterministic spell evolution visual identity samples',()=>{const a=auditSpellEvolutionIdentityAssets();assert.equal(a.samples.length,60);assert.equal(a.crestCount,8);assert.equal(a.crestCoverage,1);assert.equal(a.crestUniqueCellCount,8);assert.equal(a.heroAbilityCombinationCount,24);assert.equal(a.evolutionNameCombinationCount,48);assert.equal(a.previewCoverage,1);assert.equal(a.actionRecallCoverage,1);assert.equal(a.toastCoverage,1);assert.equal(a.textFallbackPreserved,true);assert.equal(a.imageLoadFailureNonBlocking,true);assert.equal(a.iconMotionAmplitude,0);assert.equal(a.evolutionContractMutation,false);assert.equal(a.actionCount,9);assert.equal(a.snapshotSchemaMutation,false);assert.deepEqual(a.issues,[]);assert.equal(a.passed,true);});
