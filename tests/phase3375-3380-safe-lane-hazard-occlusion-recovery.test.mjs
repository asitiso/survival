import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const u=new URL('../src/game/safe-lane-hazard-occlusion-recovery-rendering.ts',import.meta.url);const game=()=>fs.readFileSync(new URL('../src/game/game.ts',import.meta.url),'utf8');async function load(){assert.equal(fs.existsSync(u),true);return import('../dist/game/safe-lane-hazard-occlusion-recovery-rendering.js');}
test('phase 3375 safe-lane hazard occlusion recovery module exists',async()=>{const m=await load();assert.equal(typeof m.advanceSafeLaneHazardOcclusionRecovery,'function');});
test('phase 3376 a newly imminent telegraph dims the path immediately rather than easing too late',async()=>{const m=await load();const s=m.advanceSafeLaneHazardOcclusionRecovery(m.createSafeLaneHazardOcclusionRecoveryState(),{pathAlphaScale:.44,bridgeAlphaScale:.42},0,false);assert.equal(s.pathAlphaScale,.44);assert.equal(s.bridgeAlphaScale,.42);});
test('phase 3377 clearing the telegraph does not restore full route brightness on the first frame',async()=>{const m=await load();let s=m.advanceSafeLaneHazardOcclusionRecovery(m.createSafeLaneHazardOcclusionRecoveryState(),{pathAlphaScale:.44,bridgeAlphaScale:.42},0,false);s=m.advanceSafeLaneHazardOcclusionRecovery(s,{pathAlphaScale:1,bridgeAlphaScale:1},.05,false);assert.equal(s.pathAlphaScale,.44);assert.equal(s.bridgeAlphaScale,.42);});
test('phase 3378 route brightness recovers monotonically after the hold and remains bounded',async()=>{const m=await load();let s=m.advanceSafeLaneHazardOcclusionRecovery(m.createSafeLaneHazardOcclusionRecoveryState(),{pathAlphaScale:.44,bridgeAlphaScale:.42},0,false);s=m.advanceSafeLaneHazardOcclusionRecovery(s,{pathAlphaScale:1,bridgeAlphaScale:1},.2,false);const a=s.pathAlphaScale;s=m.advanceSafeLaneHazardOcclusionRecovery(s,{pathAlphaScale:1,bridgeAlphaScale:1},.2,false);assert.ok(s.pathAlphaScale>=a&&s.pathAlphaScale<=1&&s.bridgeAlphaScale<=1);});
test('phase 3379 locator ownership remains exactly one throughout hazard recovery including reduced motion',async()=>{const m=await load();for(const reduced of [false,true]){let s=m.advanceSafeLaneHazardOcclusionRecovery(m.createSafeLaneHazardOcclusionRecoveryState(),{pathAlphaScale:.44,bridgeAlphaScale:.42},0,reduced);const p=m.safeLaneHazardOcclusionRecoveryPresentation(s);assert.equal(p.locatorAlphaScale,1);}});
test('phase 3380 live safe-lane rendering advances and resets hazard recovery without changing gameplay with presentation-only audit',async()=>{const s=game();assert.match(s,/safeLaneHazardOcclusionRecoveryState/);assert.match(s,/advanceSafeLaneHazardOcclusionRecovery/);assert.match(s,/safeLaneHazardRecovery\.pathAlphaScale/);const a=await import('../dist/game/safe-lane-hazard-occlusion-recovery-audit.js');const r=a.runSafeLaneHazardOcclusionRecoveryAudit();assert.equal(r.actionCount,9);assert.equal(r.presentationOnly,true);assert.equal(r.gameplayFormulaMutation,false);assert.equal(r.snapshotSchemaMutation,false);assert.equal(r.newAtlasCount,0);assert.equal(r.passed,true);});
