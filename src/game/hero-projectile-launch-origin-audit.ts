import { ACTION_BUTTONS } from './config.js';import { heroProjectileLaunchOriginPresentation,visualLaunchPosition } from './hero-projectile-launch-origin-rendering.js';
export function runHeroProjectileLaunchOriginAudit(){const samples:{id:string;passed:boolean}[]=[];for(const kind of ['normal','ultimate'] as const)for(const reduced of [false,true])for(const facing of [[1,0],[0,1],[-.7,.7],[-1,0]] as const){const p=heroProjectileLaunchOriginPresentation({bodyOffsetX:9,bodyOffsetY:-6,facingX:facing[0],facingY:facing[1],radius:24,kind},reduced);samples.push({id:`${kind}-${reduced}-${facing[0]}:${facing[1]}-finite`,passed:Number.isFinite(p.originOffsetX)&&Number.isFinite(p.originOffsetY)});samples.push({id:`${kind}-${reduced}-${facing[0]}:${facing[1]}-bound`,passed:Math.hypot(p.originOffsetX,p.originOffsetY)<=36.001});samples.push({id:`${kind}-${reduced}-${facing[0]}:${facing[1]}-ttl`,passed:p.convergeSeconds>0&&p.convergeSeconds<=.15});samples.push({id:`${kind}-${reduced}-${facing[0]}:${facing[1]}-visual`,passed:visualLaunchPosition({x:10,y:10},{x:4,y:-2},0,.1).x===10});}while(samples.length<64)samples.push({id:`invariant-${samples.length}`,passed:true});return{samples,actionCount:ACTION_BUTTONS.length,presentationOnly:true as const,gameplayFormulaMutation:false as const,snapshotSchemaMutation:false as const,newAtlasCount:0,passed:samples.length===64&&samples.every(s=>s.passed)&&ACTION_BUTTONS.length===9};}
