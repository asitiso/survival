import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs';
const url=new URL('../dist/game/run-mission-pace-identity-assets.js',import.meta.url);
test('phase 2207 provides three static mission pace identities with frozen thresholds',async()=>{assert.equal(fs.existsSync(url),true,'run mission pace identity module must exist');const m=await import(url.href);assert.deepEqual(m.RUN_MISSION_PACE_IDENTITY_IDS,['onTrack','catchUp','critical']);assert.deepEqual(m.RUN_MISSION_PACE_IDENTITY_ATLAS,{src:'./assets/ui/run-mission-pace-icons.png',columns:3,rows:1,cellSize:96,width:288,height:96});assert.equal(m.runMissionPaceIdentityForRatios(.50,.58),'onTrack');assert.equal(m.runMissionPaceIdentityForRatios(.50,.59),'catchUp');assert.equal(m.runMissionPaceIdentityForRatios(.50,.75),'catchUp');assert.equal(m.runMissionPaceIdentityForRatios(.50,.76),'critical');for(const id of m.RUN_MISSION_PACE_IDENTITY_IDS){const icon=m.runMissionPaceIdentityIcon(id);assert.equal(icon.id,id);assert.equal(icon.maxVisibleIcons,1);assert.equal(icon.animated,false);assert.equal(icon.motionAmplitude,0);assert.equal(icon.textFallbackPreserved,true);assert.equal(icon.loadFailureBlocksGameplay,false);}const a=m.auditRunMissionPaceIdentityAtlas();assert.equal(a.coverage,1);assert.equal(a.uniqueCellCount,3);assert.deepEqual(a.outOfBounds,[]);assert.equal(a.passed,true);});
