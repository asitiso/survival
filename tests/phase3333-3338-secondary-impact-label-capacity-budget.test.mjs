import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const u=new URL('../src/game/secondary-impact-lineage-label-capacity-budget-rendering.ts',import.meta.url);const spells=()=>fs.readFileSync(new URL('../src/game/spells.ts',import.meta.url),'utf8');async function load(){assert.equal(fs.existsSync(u),true);return import('../dist/game/secondary-impact-lineage-label-capacity-budget-rendering.js');}
test('phase 3333 secondary lineage label capacity budget module exists',async()=>{const m=await load();assert.equal(typeof m.secondaryImpactLineageLabelCapacityBudget,'function');});
test('phase 3334 high quality keeps at most four already-prioritized lineage labels',async()=>{const m=await load();const r=m.secondaryImpactLineageLabelCapacityBudget(['a','b','c','d','e'],'high');assert.equal(r.maxVisible,4);assert.deepEqual(r.visibleLineageKeys,['a','b','c','d']);});
test('phase 3335 medium and low quality reduce count-label density without changing priority order',async()=>{const m=await load(),keys=['a','b','c','d'];assert.deepEqual(m.secondaryImpactLineageLabelCapacityBudget(keys,'medium').visibleLineageKeys,['a','b','c']);assert.deepEqual(m.secondaryImpactLineageLabelCapacityBudget(keys,'low').visibleLineageKeys,['a','b']);});
test('phase 3336 sparse label sets are never padded or duplicated by the capacity layer',async()=>{const m=await load();const r=m.secondaryImpactLineageLabelCapacityBudget(['x','y'],'high');assert.deepEqual(r.visibleLineageKeys,['x','y']);assert.equal(new Set(r.visibleLineageKeys).size,2);});
test('phase 3337 capacity budget is deterministic for a deterministic upstream priority order',async()=>{const m=await load(),keys=['L9','L4','L2','L1','L0'];const a=m.secondaryImpactLineageLabelCapacityBudget(keys,'medium'),b=m.secondaryImpactLineageLabelCapacityBudget([...keys],'medium');assert.deepEqual(a.visibleLineageKeys,b.visibleLineageKeys);});
test('phase 3338 SpellSystem applies capacity after lineage priority and before placement with presentation-only audit',async()=>{const s=spells();assert.match(s,/secondaryImpactLineageLabelCapacityBudget/);assert.match(s,/secondaryCapacity/);const a=await import('../dist/game/secondary-impact-lineage-label-capacity-budget-audit.js');const r=a.runSecondaryImpactLineageLabelCapacityBudgetAudit();assert.equal(r.actionCount,9);assert.equal(r.presentationOnly,true);assert.equal(r.gameplayFormulaMutation,false);assert.equal(r.snapshotSchemaMutation,false);assert.equal(r.newAtlasCount,0);assert.equal(r.passed,true);});
