import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const u=new URL('../src/game/core-guard-pressure-vector-hysteresis-rendering.ts',import.meta.url);const game=()=>fs.readFileSync(new URL('../src/game/game.ts',import.meta.url),'utf8');async function load(){assert.equal(fs.existsSync(u),true);return import('../dist/game/core-guard-pressure-vector-hysteresis-rendering.js');}
test('phase 3363 core guard pressure-vector hysteresis module exists',async()=>{const m=await load();assert.equal(typeof m.advanceCoreGuardPressureVectorHysteresis,'function');});
test('phase 3364 the first valid incoming direction is adopted as a normalized presentation vector',async()=>{const m=await load();const s=m.advanceCoreGuardPressureVectorHysteresis(m.createCoreGuardPressureVectorHysteresisState(),{x:5,y:0},0,false);assert.deepEqual(s.vector,{x:1,y:0});assert.ok(s.holdRemaining>0);});
test('phase 3365 an opposite rapid hit cannot flip the guard orientation during the hold window',async()=>{const m=await load();let s=m.advanceCoreGuardPressureVectorHysteresis(m.createCoreGuardPressureVectorHysteresisState(),{x:1,y:0},0,false);s=m.advanceCoreGuardPressureVectorHysteresis(s,{x:-1,y:0},.04,false);assert.ok(s.vector.x>.9);});
test('phase 3366 an opposite direction can take ownership after the hold window expires',async()=>{const m=await load();let s=m.advanceCoreGuardPressureVectorHysteresis(m.createCoreGuardPressureVectorHysteresisState(),{x:1,y:0},0,false);s=m.advanceCoreGuardPressureVectorHysteresis(s,{x:-1,y:0},m.CORE_GUARD_PRESSURE_VECTOR_HOLD_SECONDS+.02,false);assert.ok(s.vector.x<-.9);});
test('phase 3367 reduced motion snaps to the new direction instead of animating a directional blend',async()=>{const m=await load();let s=m.advanceCoreGuardPressureVectorHysteresis(m.createCoreGuardPressureVectorHysteresisState(),{x:1,y:0},0,true);s=m.advanceCoreGuardPressureVectorHysteresis(s,{x:0,y:1},.02,true);assert.ok(Math.abs(s.vector.x)<1e-9&&s.vector.y>.99);});
test('phase 3368 live core-hit queue stabilizes pressure vectors and resets state per run with presentation-only audit',async()=>{const s=game();assert.match(s,/coreGuardPressureVectorHysteresisState/);assert.match(s,/advanceCoreGuardPressureVectorHysteresis/);assert.match(s,/createCoreGuardPressureVectorHysteresisState\(\)/);const a=await import('../dist/game/core-guard-pressure-vector-hysteresis-audit.js');const r=a.runCoreGuardPressureVectorHysteresisAudit();assert.equal(r.actionCount,9);assert.equal(r.presentationOnly,true);assert.equal(r.gameplayFormulaMutation,false);assert.equal(r.snapshotSchemaMutation,false);assert.equal(r.newAtlasCount,0);assert.equal(r.passed,true);});
