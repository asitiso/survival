import { ACTION_BUTTONS } from './config.js';import { coreGuardMixedAccentPhasePresentation } from './core-guard-mixed-accent-phase-rendering.js';
export function runCoreGuardMixedAccentPhaseAudit(){const samples:{id:string;passed:boolean}[]=[];for(const reduced of [false,true])for(const mixedPressure of [false,true])for(const life of [.08,.28,.58,.92]){const p=coreGuardMixedAccentPhasePresentation({ttl:life,maxTtl:1,mixedPressure},reduced);samples.push({id:`${reduced}-${mixedPressure}-${life}`,passed:p.projectileAlphaScale>0&&p.projectileAlphaScale<=1&&p.contactAlphaScale>0&&p.contactAlphaScale<=1&&p.projectileLengthScale>.8&&p.projectileLengthScale<1.2&&p.contactRadiusScale>.8&&p.contactRadiusScale<1.2});}return{samples,actionCount:ACTION_BUTTONS.length,presentationOnly:true as const,gameplayFormulaMutation:false as const,snapshotSchemaMutation:false as const,newAtlasCount:0,passed:samples.length===16&&samples.every(s=>s.passed)&&ACTION_BUTTONS.length===9};}
