import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const u=new URL('../src/game/safe-lane-forecast-promotion-hysteresis-rendering.ts',import.meta.url);const game=()=>fs.readFileSync(new URL('../src/game/game.ts',import.meta.url),'utf8');async function load(){assert.equal(fs.existsSync(u),true);return import('../dist/game/safe-lane-forecast-promotion-hysteresis-rendering.js');}
test('phase 3267 safe-lane forecast promotion hysteresis module exists',async()=>{const m=await load();assert.equal(typeof m.advanceSafeLaneForecastPromotionHysteresis,'function');});
test('phase 3268 promotion requires a stronger enter threshold than the raw forecast seam',async()=>{const m=await load();let s=m.createSafeLaneForecastPromotionHysteresisState();s=m.advanceSafeLaneForecastPromotionHysteresis(s,{hasForecast:true,urgency:.66,transitionMs:900,targetDistance:140});assert.equal(s.owner,'current');s=m.advanceSafeLaneForecastPromotionHysteresis(s,{hasForecast:true,urgency:.72,transitionMs:900,targetDistance:140});assert.equal(s.owner,'forecast');});
test('phase 3269 once promoted a small urgency dip does not ping-pong back to current',async()=>{const m=await load();let s=m.advanceSafeLaneForecastPromotionHysteresis(m.createSafeLaneForecastPromotionHysteresisState(),{hasForecast:true,urgency:.74,transitionMs:900,targetDistance:140});s=m.advanceSafeLaneForecastPromotionHysteresis(s,{hasForecast:true,urgency:.61,transitionMs:1200,targetDistance:140});assert.equal(s.owner,'forecast');});
test('phase 3270 settled target equality always returns ownership to current',async()=>{const m=await load();let s=m.advanceSafeLaneForecastPromotionHysteresis(m.createSafeLaneForecastPromotionHysteresisState(),{hasForecast:true,urgency:.9,transitionMs:500,targetDistance:140});s=m.advanceSafeLaneForecastPromotionHysteresis(s,{hasForecast:true,urgency:.9,transitionMs:0,targetDistance:4});assert.equal(s.owner,'current');});
test('phase 3271 missing forecast immediately leaves current authoritative',async()=>{const m=await load();let s=m.advanceSafeLaneForecastPromotionHysteresis(m.createSafeLaneForecastPromotionHysteresisState(),{hasForecast:true,urgency:.9,transitionMs:500,targetDistance:140});s=m.advanceSafeLaneForecastPromotionHysteresis(s,{hasForecast:false,urgency:0,transitionMs:0,targetDistance:Infinity});assert.equal(s.owner,'current');});
test('phase 3272 live mythic safe-lane coherence consumes hysteresis owner without gameplay mutation',async()=>{const s=game();assert.match(s,/safeLaneForecastPromotionHysteresisState/);assert.match(s,/advanceSafeLaneForecastPromotionHysteresis/);assert.match(s,/promotionOwner/);const a=await import('../dist/game/safe-lane-forecast-promotion-hysteresis-audit.js');const r=a.runSafeLaneForecastPromotionHysteresisAudit();assert.equal(r.actionCount,9);assert.equal(r.presentationOnly,true);assert.equal(r.gameplayFormulaMutation,false);assert.equal(r.snapshotSchemaMutation,false);assert.equal(r.passed,true);});
