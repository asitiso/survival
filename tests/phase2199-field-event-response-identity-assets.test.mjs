import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs';
const url=new URL('../dist/game/field-event-response-identity-assets.js',import.meta.url);
test('phase 2199 provides five static field-event response identities',async()=>{assert.equal(fs.existsSync(url),true,'field-event response identity module must exist');const m=await import(url.href);assert.deepEqual(m.FIELD_EVENT_RESPONSE_IDENTITY_IDS,['chase','retrieve','overcast','harvest','burst-control']);assert.deepEqual(m.FIELD_EVENT_RESPONSE_IDENTITY_ATLAS,{src:'./assets/ui/field-event-response-icons.png',columns:5,rows:1,cellSize:96,width:480,height:96});assert.equal(m.fieldEventResponseIdentityForEvent('goldenGoblin'),'chase');assert.equal(m.fieldEventResponseIdentityForEvent('supplyDrop'),'retrieve');assert.equal(m.fieldEventResponseIdentityForEvent('manaStorm'),'overcast');assert.equal(m.fieldEventResponseIdentityForEvent('goldenNight'),'harvest');assert.equal(m.fieldEventResponseIdentityForEvent('eliteRush'),'burst-control');for(const id of m.FIELD_EVENT_RESPONSE_IDENTITY_IDS){const icon=m.fieldEventResponseIdentityIcon(id);assert.equal(icon.id,id);assert.equal(icon.maxVisibleIcons,1);assert.equal(icon.animated,false);assert.equal(icon.motionAmplitude,0);assert.equal(icon.textFallbackPreserved,true);assert.equal(icon.loadFailureBlocksGameplay,false);}const a=m.auditFieldEventResponseIdentityAtlas();assert.equal(a.coverage,1);assert.equal(a.uniqueCellCount,5);assert.deepEqual(a.outOfBounds,[]);assert.equal(a.passed,true);});
