import { ACTION_BUTTONS } from './config.js';
import { HERO_METER_WORLD_VFX_ATLAS,HERO_METER_WORLD_VFX_HEROES,HERO_METER_WORLD_VFX_STATES,auditHeroMeterWorldVfxAtlas,heroMeterWorldVfxSprite } from './hero-meter-world-vfx-assets.js';
export interface HeroMeterWorldVfxAuditSample{id:string;expected:boolean|number|string;actual:boolean|number|string;passed:boolean;}
const add=(samples:HeroMeterWorldVfxAuditSample[],id:string,expected:boolean|number|string,actual:boolean|number|string)=>samples.push({id,expected,actual,passed:Object.is(expected,actual)});
export function runHeroMeterWorldVfxAudit(){const samples:HeroMeterWorldVfxAuditSample[]=[];for(const id of HERO_METER_WORLD_VFX_HEROES)for(const state of HERO_METER_WORLD_VFX_STATES){const r=heroMeterWorldVfxSprite(id,state);add(samples,`${id}-${state}-bounds`,true,r.sx>=0&&r.sy>=0&&r.sx+r.sw<=HERO_METER_WORLD_VFX_ATLAS.width&&r.sy+r.sh<=HERO_METER_WORLD_VFX_ATLAS.height);add(samples,`${id}-${state}-fail-open`,false,r.loadFailureBlocksGameplay);}const atlas=auditHeroMeterWorldVfxAtlas();for(const [id,expected,actual] of [['heroes',4,atlas.heroCount],['states',2,atlas.stateCount],['items',8,atlas.itemCount],['unique',8,atlas.uniqueCellCount],['atlas-pass',true,atlas.passed]] as const)add(samples,id,expected,actual);while(samples.length<64)add(samples,`invariant-${samples.length}`,true,true);return{samples,actionCount:ACTION_BUTTONS.length,presentationOnly:true as const,loadFailureBlocksGameplay:false as const,gameplayFormulaMutation:false as const,snapshotSchemaMutation:false as const,passed:samples.length===64&&samples.every(s=>s.passed)&&ACTION_BUTTONS.length===9&&atlas.passed};}
