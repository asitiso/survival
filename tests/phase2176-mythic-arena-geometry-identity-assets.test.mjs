import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs';
const url=new URL('../dist/game/endless/mythic-arena-geometry-identity-assets.js',import.meta.url);
test('phase 2176 provides six static Mythic arena geometry identities',async()=>{assert.equal(fs.existsSync(url),true,'mythic arena geometry identity module must exist');const m=await import(url.href);assert.equal(m.MYTHIC_ARENA_GEOMETRY_IDENTITY_IDS.length,6);assert.deepEqual(m.MYTHIC_ARENA_GEOMETRY_IDENTITY_ATLAS,{src:'./assets/bosses/mythic-arena-geometry-icons.png',columns:3,rows:2,cellSize:96,width:288,height:192});for(const id of m.MYTHIC_ARENA_GEOMETRY_IDENTITY_IDS){const icon=m.mythicArenaGeometryIdentityIcon(id);assert.equal(icon.id,id);assert.equal(icon.safeLaneIdentitySupported,true);assert.equal(icon.weakpointReliefIdentityStable,true);assert.equal(icon.maxVisibleIcons,1);assert.equal(icon.animated,false);assert.equal(icon.motionAmplitude,0);assert.equal(icon.textFallbackPreserved,true);assert.equal(icon.loadFailureBlocksGameplay,false);}const a=m.auditMythicArenaGeometryIdentityAtlas();assert.equal(a.coverage,1);assert.equal(a.uniqueCellCount,6);assert.deepEqual(a.outOfBounds,[]);assert.equal(a.passed,true);});
