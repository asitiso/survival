import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs';
const url=new URL('../dist/game/objective-reward-identity-assets.js',import.meta.url);
test('phase 2192 provides four static objective reward identities and deterministic scaled preview amounts',async()=>{assert.equal(fs.existsSync(url),true,'objective reward identity module must exist');const m=await import(url.href);assert.deepEqual(m.OBJECTIVE_REWARD_IDENTITY_IDS,['gold','shopToken','potion','temporaryPower']);assert.deepEqual(m.OBJECTIVE_REWARD_IDENTITY_ATLAS,{src:'./assets/ui/objective-reward-icons.png',columns:4,rows:1,cellSize:96,width:384,height:96});assert.equal(m.objectiveRewardPreviewAmount({kind:'gold',amount:120},1.12),134);assert.equal(m.objectiveRewardPreviewAmount({kind:'shopToken',amount:1},1.12),1);assert.equal(m.objectiveRewardPreviewAmount({kind:'potion',amount:1},1.35),1);assert.equal(m.objectiveRewardPreviewAmount({kind:'temporaryPower',amount:20},1.35),20);for(const id of m.OBJECTIVE_REWARD_IDENTITY_IDS){const icon=m.objectiveRewardIdentityIcon(id);assert.equal(icon.id,id);assert.equal(icon.maxPreviewIcons,2);assert.equal(icon.animated,false);assert.equal(icon.motionAmplitude,0);assert.equal(icon.loadFailureBlocksGameplay,false);}const a=m.auditObjectiveRewardIdentityAtlas();assert.equal(a.coverage,1);assert.equal(a.uniqueCellCount,4);assert.deepEqual(a.outOfBounds,[]);assert.equal(a.passed,true);});
