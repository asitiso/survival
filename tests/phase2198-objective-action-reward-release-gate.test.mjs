import test from 'node:test'; import assert from 'node:assert/strict';
import { auditReleaseFreeze } from '../dist/game/release-freeze-audit.js';
import { releaseCandidateAudit } from '../dist/game/release-candidate-audit.js';
test('phase 2198 release freeze binds objective action/reward identity evidence',()=>{const f=auditReleaseFreeze();assert.equal(f.objectiveActionRewardIdentityAssetsPassed,true);assert.equal(f.objectiveActionRewardIdentityAssetsSamples,60);assert.equal(f.passed,true);});
test('phase 2198 candidate fails closed on forged objective identity evidence and sample mutation changes signature',()=>{const base=releaseCandidateAudit();const forged=structuredClone(base.evidence);forged.releaseFreeze.objectiveActionRewardIdentityAssetsPassed=false;forged.releaseFreeze.passed=true;const bad=releaseCandidateAudit(forged);assert.equal(bad.status,'REVIEW');assert.ok(bad.issues.includes('release-freeze'));const changed=structuredClone(base.evidence);changed.releaseFreeze.objectiveActionRewardIdentityAssetsSamples+=1;const mutated=releaseCandidateAudit(changed);assert.notEqual(mutated.signature,base.signature);assert.match(base.markdown,/objective-action-reward-identity-assets safe \(60\)/);});
