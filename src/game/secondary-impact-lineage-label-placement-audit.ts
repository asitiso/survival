import { ACTION_BUTTONS, LOGICAL_HEIGHT, LOGICAL_WIDTH } from './config.js';
import { SECONDARY_IMPACT_LABEL_PRIMARY_CLEARANCE, SECONDARY_IMPACT_LABEL_SCREEN_INSET, secondaryImpactLineageLabelPlacement } from './secondary-impact-lineage-label-placement-rendering.js';
export function runSecondaryImpactLineageLabelPlacementAudit(){const samples:{id:string;passed:boolean}[]=[];for(const edge of [false,true])for(const blocked of [false,true])for(let i=0;i<8;i++){const anchor=edge?{x:i%2?LOGICAL_WIDTH-4:4,y:i%3?10:LOGICAL_HEIGHT-6}:{x:220+i*58,y:180+(i%3)*84},blockers=blocked?[{x:anchor.x,y:anchor.y-30}]:[],p=secondaryImpactLineageLabelPlacement({anchor,blockers,occupied:[],width:LOGICAL_WIDTH,height:LOGICAL_HEIGHT});const inBounds=p.pos.x>=SECONDARY_IMPACT_LABEL_SCREEN_INSET&&p.pos.x<=LOGICAL_WIDTH-SECONDARY_IMPACT_LABEL_SCREEN_INSET&&p.pos.y>=SECONDARY_IMPACT_LABEL_SCREEN_INSET&&p.pos.y<=LOGICAL_HEIGHT-SECONDARY_IMPACT_LABEL_SCREEN_INSET,clear=!p.visible||blockers.every(b=>Math.hypot(p.pos.x-b.x,p.pos.y-b.y)>=SECONDARY_IMPACT_LABEL_PRIMARY_CLEARANCE);samples.push({id:`${edge}-${blocked}-${i}`,passed:inBounds&&clear});}return{samples,actionCount:ACTION_BUTTONS.length,presentationOnly:true as const,gameplayFormulaMutation:false as const,snapshotSchemaMutation:false as const,newAtlasCount:0,passed:samples.length===32&&samples.every(s=>s.passed)&&ACTION_BUTTONS.length===9};}
