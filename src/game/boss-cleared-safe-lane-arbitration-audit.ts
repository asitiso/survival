import { ACTION_BUTTONS } from './config.js';
import { bossClearedSafeLaneArbitrationPresentation } from './boss-cleared-safe-lane-arbitration-rendering.js';
export function runBossClearedSafeLaneArbitrationAudit(){const samples:{id:string;passed:boolean}[]=[];for(const shape of ['circle','corridor','cross'] as const)for(const reduced of [false,true])for(const target of [{x:500,y:310},{x:410,y:440},{x:400,y:300}]){const p=bossClearedSafeLaneArbitrationPresentation({shape,center:{x:400,y:300},angle:0,halfLength:160,halfWidth:28,safeLaneTarget:target,safeLaneConfidence:.82},reduced);samples.push({id:`${shape}-${reduced}-${target.x}-${target.y}`,passed:p.clearedAlphaScale>=0&&p.clearedAlphaScale<=1&&p.safeLaneAlphaScale>=0&&p.safeLaneAlphaScale<=1});}return{samples,actionCount:ACTION_BUTTONS.length,presentationOnly:true as const,gameplayFormulaMutation:false as const,snapshotSchemaMutation:false as const,newAtlasCount:0,passed:samples.length===18&&samples.every(s=>s.passed)&&ACTION_BUTTONS.length===9};}
