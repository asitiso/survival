const clamp01=(value:number)=>Math.max(0,Math.min(1,Number.isFinite(value)?value:0));
export function denseProjectileLocalMaterialPresentation(input:{stress:number;laneProximity:number;critical:boolean},reducedMotion=false,reducedFlash=false){const footprint=clamp01(Math.max(input.stress,input.laneProximity*.92)),floor=input.critical?.78:.36,trailCeiling=Math.max(floor,1-footprint*(input.critical?.18:.64))*(reducedMotion?.96:1)*(reducedFlash?.97:1);return{footprint,canonicalBodyScale:1,trailCeiling,presentationOnly:true as const};}
export function denseImpactLocalMaterialPresentation(input:{stress:number;heroProximity:number;critical:boolean},reducedFlash=false){const footprint=clamp01(Math.max(input.stress,input.heroProximity*.94)),floor=input.critical?.56:.28,fillCeiling=Math.max(floor,1-footprint*(input.critical?.3:.72))*(reducedFlash?.96:1);return{footprint,canonicalBodyScale:1,fillCeiling,presentationOnly:true as const};}
export function denseHazardLocalMaterialPresentation(input:{stress:number;laneProximity:number;telegraph:boolean;critical:boolean},reducedFlash=false){const footprint=clamp01(Math.max(input.stress,input.laneProximity*.96)),floor=input.critical?.58:input.telegraph?.34:.3,cut=input.critical?.28:input.telegraph?.66:.62,fillCeiling=Math.max(floor,1-footprint*cut)*(reducedFlash?.96:1);return{footprint,telegraphEdgeScale:1,fillCeiling,presentationOnly:true as const};}
export function denseSafeLaneLocalMaterialPresentation(input:{load:number;confidence:number;critical:boolean},reducedFlash=false){const load=clamp01(input.load),confidence=clamp01(input.confidence),focus=clamp01(load+(input.critical?.12:0)+confidence*.08),secondaryCeiling=Math.max(.48,1-focus*.52)*(reducedFlash?.97:1);return{focus,pathAlphaFloor:Math.max(input.critical?.94:.92,.92+confidence*.06),safeLaneScale:1,secondaryCeiling,presentationOnly:true as const};}
export function denseSilhouetteLocalMaterialPresentation(input:{stress:number;owner:'locomotion'|'attack'|'recovery'|'hit'|'special';critical:boolean},reducedMotion=false){const stress=clamp01(input.stress),ownerWeight=input.owner==='recovery'?1:input.owner==='locomotion'?.82:input.owner==='hit'?.68:input.owner==='attack'?.54:.46,floor=input.critical?.72:input.owner==='recovery'?.46:.5,trailCeiling=Math.max(floor,1-stress*(.42+.18*ownerWeight))*(reducedMotion?.96:1);return{stress,canonicalBodyScale:1,trailCeiling,presentationOnly:true as const};}
export function localMaterialBudgetPresentation(input:{stress:number;criticalCount:number;safeLaneVisible:boolean;bossTelegraphActive:boolean},reducedMotion=false,reducedFlash=false){const stress=clamp01(input.stress),critical=clamp01(Math.max(0,input.criticalCount)/3),reserve=(input.safeLaneVisible?.12:0)+(input.bossTelegraphActive?.1:0),secondaryCeiling=Math.max(.48,1-stress*.42-critical*.04-reserve)*(reducedMotion?.98:1)*(reducedFlash?.97:1);return{stress,criticalScale:1,canonicalBodyScale:1,bossTelegraphEdgeScale:1,safeLaneScale:1,secondaryCeiling:clamp01(secondaryCeiling),presentationOnly:true as const};}
