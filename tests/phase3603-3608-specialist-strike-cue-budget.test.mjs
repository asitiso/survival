import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
import { specialistStrikeCueBudgetPresentation } from '../dist/game/specialist-strike-cue-budget-rendering.js';
test('sparse strike cues stay fully visible',()=>{const p=specialistStrikeCueBudgetPresentation({activeCueCount:2,indexFromNewest:0,type:'assassin',life:.8},false,false);assert.equal(p.visible,true);assert.equal(p.alphaScale,1);});
test('dense packs keep newest cues first',()=>{const n=specialistStrikeCueBudgetPresentation({activeCueCount:9,indexFromNewest:1,type:'shieldbearer',life:.8},false,false),o=specialistStrikeCueBudgetPresentation({activeCueCount:9,indexFromNewest:7,type:'shieldbearer',life:.8},false,false);assert.equal(n.visible,true);assert.equal(o.visible,false);});
test('assassin strike identity keeps slightly more alpha in crowd',()=>{const a=specialistStrikeCueBudgetPresentation({activeCueCount:7,indexFromNewest:2,type:'assassin',life:.7},false,false),s=specialistStrikeCueBudgetPresentation({activeCueCount:7,indexFromNewest:2,type:'shieldbearer',life:.7},false,false);assert.ok(a.alphaScale>=s.alphaScale);});
test('old life phase loses alpha before visibility slot',()=>{const a=specialistStrikeCueBudgetPresentation({activeCueCount:5,indexFromNewest:1,type:'siegeGolem',life:.9},false,false),b=specialistStrikeCueBudgetPresentation({activeCueCount:5,indexFromNewest:1,type:'siegeGolem',life:.2},false,false);assert.ok(b.alphaScale<a.alphaScale);});
test('reduced flash lowers alpha without changing visibility owner',()=>{const f=specialistStrikeCueBudgetPresentation({activeCueCount:6,indexFromNewest:1,type:'assassin',life:.8},false,false),r=specialistStrikeCueBudgetPresentation({activeCueCount:6,indexFromNewest:1,type:'assassin',life:.8},false,true);assert.equal(f.visible,r.visible);assert.ok(r.alphaScale<f.alphaScale);});
test('live render applies strike cue budget before drawing',()=>{const src=fs.readFileSync('src/game/enemies.ts','utf8');assert.match(src,/specialistStrikeCueBudgetPresentation/);assert.match(src,/strikeCueBudget\.visible/);});
