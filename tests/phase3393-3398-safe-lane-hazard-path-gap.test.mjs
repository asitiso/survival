import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const u=new URL('../src/game/safe-lane-hazard-path-gap-rendering.ts',import.meta.url);const game=()=>fs.readFileSync(new URL('../src/game/game.ts',import.meta.url),'utf8');async function load(){assert.equal(fs.existsSync(u),true);return import('../dist/game/safe-lane-hazard-path-gap-rendering.js');}
test('phase 3393 safe-lane hazard path gap module exists',async()=>{const m=await load();assert.equal(typeof m.safeLaneHazardPathGapPresentation,'function');});
test('phase 3394 a clear route remains one continuous segment',async()=>{const m=await load();const p=m.safeLaneHazardPathGapPresentation({from:{x:100,y:100},to:{x:500,y:100},hazards:[]});assert.equal(p.gapApplied,false);assert.equal(p.segments.length,1);assert.deepEqual(p.segments[0],{from:{x:100,y:100},to:{x:500,y:100}});});
test('phase 3395 an imminent telegraph crossing the route creates at most two bounded path segments',async()=>{const m=await load();const p=m.safeLaneHazardPathGapPresentation({from:{x:100,y:100},to:{x:500,y:100},hazards:[{pos:{x:300,y:100},radius:55,telegraph:.18}]});assert.equal(p.gapApplied,true);assert.ok(p.segments.length>=1&&p.segments.length<=2);assert.equal(p.locatorVisible,true);});
test('phase 3396 prepare-stage or off-route telegraphs do not punch unnecessary holes in navigation',async()=>{const m=await load();for(const h of [{pos:{x:300,y:100},radius:55,telegraph:.9},{pos:{x:300,y:300},radius:55,telegraph:.18}]){const p=m.safeLaneHazardPathGapPresentation({from:{x:100,y:100},to:{x:500,y:100},hazards:[h]});assert.equal(p.gapApplied,false);assert.equal(p.segments.length,1);}});
test('phase 3397 the safe-lane target locator stays external to route segmentation even when the gap reaches the destination side',async()=>{const m=await load();const p=m.safeLaneHazardPathGapPresentation({from:{x:100,y:100},to:{x:500,y:100},hazards:[{pos:{x:470,y:100},radius:70,telegraph:.12}]});assert.equal(p.locatorVisible,true);assert.ok(p.segments.every(seg=>Number.isFinite(seg.from.x)&&Number.isFinite(seg.to.x)));});
test('phase 3398 live safe-lane route draws gap segments while preserving the existing target locator with presentation-only audit',async()=>{const s=game();assert.match(s,/safeLaneHazardPathGapPresentation/);assert.match(s,/safeLanePathGap\.segments/);assert.match(s,/safeLanePathGap\.locatorVisible/);const a=await import('../dist/game/safe-lane-hazard-path-gap-audit.js');const r=a.runSafeLaneHazardPathGapAudit();assert.equal(r.actionCount,9);assert.equal(r.presentationOnly,true);assert.equal(r.gameplayFormulaMutation,false);assert.equal(r.snapshotSchemaMutation,false);assert.equal(r.newAtlasCount,0);assert.equal(r.passed,true);});
