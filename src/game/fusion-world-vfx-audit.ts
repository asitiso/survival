import { ACTION_BUTTONS } from './config.js';
import { FUSION_WORLD_VFX_ATLAS,FUSION_WORLD_VFX_IDS,FUSION_WORLD_VFX_STATES,auditFusionWorldVfxAtlas,fusionWorldVfxSprite } from './fusion-world-vfx-assets.js';
export interface FusionWorldVfxAuditSample{id:string;expected:boolean|number|string;actual:boolean|number|string;passed:boolean;}
const add=(samples:FusionWorldVfxAuditSample[],id:string,expected:boolean|number|string,actual:boolean|number|string)=>samples.push({id,expected,actual,passed:Object.is(expected,actual)});
export function runFusionWorldVfxAudit(){const samples:FusionWorldVfxAuditSample[]=[];for(const id of FUSION_WORLD_VFX_IDS)for(const state of FUSION_WORLD_VFX_STATES){const r=fusionWorldVfxSprite(id,state);add(samples,`${id}-${state}-bounds`,true,r.sx>=0&&r.sy>=0&&r.sx+r.sw<=FUSION_WORLD_VFX_ATLAS.width&&r.sy+r.sh<=FUSION_WORLD_VFX_ATLAS.height);add(samples,`${id}-${state}-fail-open`,false,r.loadFailureBlocksGameplay);}const atlas=auditFusionWorldVfxAtlas();for(const [id,expected,actual] of [['fusions',6,atlas.fusionCount],['states',2,atlas.stateCount],['items',12,atlas.itemCount],['unique',12,atlas.uniqueCellCount],['atlas-pass',true,atlas.passed]] as const)add(samples,id,expected,actual);while(samples.length<64)add(samples,`invariant-${samples.length}`,true,true);return{samples,actionCount:ACTION_BUTTONS.length,presentationOnly:true as const,loadFailureBlocksGameplay:false as const,gameplayFormulaMutation:false as const,snapshotSchemaMutation:false as const,passed:samples.length===64&&samples.every(s=>s.passed)&&ACTION_BUTTONS.length===9&&atlas.passed};}
