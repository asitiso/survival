import test from 'node:test'; import assert from 'node:assert/strict';
import { auditFieldEventResponseEffectIdentityAssets } from '../dist/game/field-event-response-effect-identity-audit.js';
test('phase 2205 audits exactly 60 deterministic field-event response/effect samples',()=>{const a=auditFieldEventResponseEffectIdentityAssets();assert.equal(a.samples.length,60);assert.equal(a.responseIdentityCount,5);assert.equal(a.effectIdentityCount,5);assert.equal(a.responseCoverage,1);assert.equal(a.effectCoverage,1);assert.equal(a.responseUniqueCellCount,5);assert.equal(a.effectUniqueCellCount,5);assert.deepEqual(a.eventDurations,[22,30,25,30,14]);assert.deepEqual(a.scheduleContract,{firstEventAt:75,bossSafetyWindow:12,minBetweenEvents:85,maxBetweenEvents:120});assert.equal(a.eliteRushMaxCount,12);assert.equal(a.actionCount,9);assert.equal(a.snapshotSchemaMutation,false);assert.equal(a.gameplayContractMutation,false);assert.deepEqual(a.issues,[]);assert.equal(a.passed,true);assert.equal(a.samples.every(sample=>sample.passed),true);});
