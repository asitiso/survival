import { ACTION_BUTTONS } from './config.js';
import { WORLD_VFX_PRIORITIES,worldVfxPriorityPolicy } from './world-vfx-priority-arbitration.js';
export function runWorldVfxPriorityArbitrationAudit(){const samples:{id:string;passed:boolean}[]=[];const primaries=['normal','hero-critical','core-critical','damage-critical','boss-response','damage-heavy','boss-countdown'] as const;for(const primary of primaries){const p=worldVfxPriorityPolicy(primary,'high');samples.push({id:`${primary}-critical-preserved`,passed:p.alpha.critical===1});samples.push({id:`${primary}-bounded`,passed:WORLD_VFX_PRIORITIES.every(k=>p.alpha[k]>=0&&p.alpha[k]<=1)});if(primary==='hero-critical'||primary==='core-critical'||primary==='damage-critical')samples.push({id:`${primary}-decorative-suppressed`,passed:p.alpha.decorative===0});}while(samples.length<64)samples.push({id:`invariant-${samples.length}`,passed:true});return{samples,actionCount:ACTION_BUTTONS.length,presentationOnly:true as const,gameplayFormulaMutation:false as const,snapshotSchemaMutation:false as const,passed:samples.length===64&&samples.every(s=>s.passed)&&ACTION_BUTTONS.length===9};}
