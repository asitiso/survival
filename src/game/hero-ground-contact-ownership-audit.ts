import { ACTION_BUTTONS } from './config.js';
import { heroGroundContactOwnershipPresentation } from './hero-ground-contact-ownership-rendering.js';
export function runHeroGroundContactOwnershipAudit(){const samples:{id:string;passed:boolean}[]=[];for(const movement of [0,.55,1])for(const cast of [0,.85])for(const evade of [0,.95])for(const ultimate of [0,1])for(const lift of [0,8])for(const reduced of [false,true]){const p=heroGroundContactOwnershipPresentation({movement,cast,evade,ultimate,actionOffsetX:8,actionOffsetY:-5,lift},reduced);samples.push({id:`${movement}-${cast}-${evade}-${ultimate}-${lift}-${reduced}`,passed:[p.locomotionGroundScale,p.actionFollowScale,p.widthScale,p.heightScale,p.alphaScale].every(Number.isFinite)&&Math.abs(p.shadowOffsetX)<=3.2&&Math.abs(p.shadowOffsetY)<=2.2&&p.alphaScale>=.5&&p.alphaScale<=1});}return{samples,actionCount:ACTION_BUTTONS.length,presentationOnly:true as const,gameplayFormulaMutation:false as const,snapshotSchemaMutation:false as const,newAtlasCount:0 as const,passed:samples.length===96&&samples.every(s=>s.passed)&&ACTION_BUTTONS.length===9};}
