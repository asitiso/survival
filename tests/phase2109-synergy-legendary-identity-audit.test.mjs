import test from 'node:test';import assert from 'node:assert/strict';
import { auditSynergyLegendaryIdentityAssets } from '../dist/game/synergy-legendary-identity-asset-audit.js';
test('phase 2109 audits exactly sixty deterministic synergy and legendary identity samples',()=>{const a=auditSynergyLegendaryIdentityAssets();assert.equal(a.samples.length,60);assert.equal(a.synergyCount,10);assert.equal(a.synergyCoverage,1);assert.equal(a.synergyUniqueCellCount,10);assert.equal(a.legendaryItemCount,8);assert.equal(a.legendaryReuseCoverage,1);assert.equal(a.sustainedRecallCoverage,1);assert.equal(a.procToastCoverage,1);assert.equal(a.maxSynergyRecallIcons,2);assert.equal(a.maxLegendaryRecallIcons,2);assert.equal(a.textFallbackPreserved,true);assert.equal(a.imageLoadFailureNonBlocking,true);assert.equal(a.iconMotionAmplitude,0);assert.equal(a.synergyContractMutation,false);assert.equal(a.legendaryContractMutation,false);assert.equal(a.actionCount,9);assert.equal(a.snapshotSchemaMutation,false);assert.deepEqual(a.issues,[]);assert.equal(a.passed,true);});
