import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs';
const url=new URL('../dist/game/endless/safe-zone-transition-direction-assets.js',import.meta.url);
test('phase 2184 provides eight static transition directions and deterministic vector quantization',async()=>{assert.equal(fs.existsSync(url),true,'transition direction asset module must exist');const m=await import(url.href);assert.deepEqual(m.SAFE_ZONE_TRANSITION_DIRECTION_IDS,['N','NE','E','SE','S','SW','W','NW']);assert.deepEqual(m.SAFE_ZONE_TRANSITION_DIRECTION_ATLAS,{src:'./assets/bosses/safe-zone-transition-direction-icons.png',columns:4,rows:2,cellSize:96,width:384,height:192});const cases=[[0,-10,'N'],[8,-8,'NE'],[9,0,'E'],[8,8,'SE'],[0,7,'S'],[-8,8,'SW'],[-9,0,'W'],[-8,-8,'NW']];for(const [dx,dy,id] of cases){assert.equal(m.safeZoneTransitionDirectionFromVector(dx,dy),id);const icon=m.safeZoneTransitionDirectionIcon(id);assert.equal(icon.direction,id);assert.equal(icon.forecastUrgencyThreshold,.65);assert.equal(icon.autoMove,false);assert.equal(icon.animated,false);assert.equal(icon.motionAmplitude,0);}const a=m.auditSafeZoneTransitionDirectionAtlas();assert.equal(a.coverage,1);assert.equal(a.uniqueCellCount,8);assert.deepEqual(a.outOfBounds,[]);assert.equal(a.passed,true);});
