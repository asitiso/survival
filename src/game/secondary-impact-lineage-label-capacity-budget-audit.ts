import { ACTION_BUTTONS } from './config.js';import { secondaryImpactLineageLabelCapacityBudget, type SecondaryImpactLabelCapacityQuality } from './secondary-impact-lineage-label-capacity-budget-rendering.js';
export function runSecondaryImpactLineageLabelCapacityBudgetAudit(){const samples:{id:string;passed:boolean}[]=[];for(const quality of ['low','medium','high'] as SecondaryImpactLabelCapacityQuality[])for(let count=0;count<8;count++){const keys=Array.from({length:count},(_,i)=>`L${i}`),r=secondaryImpactLineageLabelCapacityBudget(keys,quality);samples.push({id:`${quality}-${count}`,passed:r.visibleLineageKeys.length<=r.maxVisible&&r.visibleLineageKeys.every((k,i)=>k===keys[i])&&new Set(r.visibleLineageKeys).size===r.visibleLineageKeys.length});}return{samples,actionCount:ACTION_BUTTONS.length,presentationOnly:true as const,gameplayFormulaMutation:false as const,snapshotSchemaMutation:false as const,newAtlasCount:0,passed:samples.length===24&&samples.every(s=>s.passed)&&ACTION_BUTTONS.length===9};}
