export interface ArchiveProvenanceInput{sourceRevision:string;archiveComment:string;criticalFileCount:number;matchingCriticalFiles:number;dirty:boolean;archiveErrors:number;}
export interface ArchiveProvenanceAudit extends ArchiveProvenanceInput{criticalFileCoverage:number;passed:boolean;issues:string[];}
export function evaluateArchiveProvenance(input:ArchiveProvenanceInput):ArchiveProvenanceAudit{const issues:string[]=[];if(input.sourceRevision!==input.archiveComment)issues.push('revision-mismatch');if(input.criticalFileCount<=0||input.matchingCriticalFiles!==input.criticalFileCount)issues.push('critical-file-drift');if(input.dirty)issues.push('source-dirty');if(input.archiveErrors>0)issues.push('archive-errors');const criticalFileCoverage=input.criticalFileCount>0?input.matchingCriticalFiles/input.criticalFileCount:0;return{...input,criticalFileCoverage,passed:issues.length===0,issues};}
