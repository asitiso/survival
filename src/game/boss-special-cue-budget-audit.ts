import { ACTION_BUTTONS } from './config.js';import { bossSpecialCueBudgetPresentation } from './boss-special-cue-budget-rendering.js';import type { BossPhase } from './boss-patterns.js';
export function runBossSpecialCueBudgetAudit(){const samples:{id:string;passed:boolean}[]=[];for(const phase of [1,2,3] as BossPhase[])for(const mode of ['special','recovery','stagger'] as const)for(const reduced of [false,true]){const p=bossSpecialCueBudgetPresentation({phase,charge:mode==='special'?.8:.08,recovery:mode==='recovery'?.8:0,stagger:mode==='stagger'?.8:0,phaseOverlay:true},reduced,reduced);samples.push({id:`${phase}-${mode}-${reduced}`,passed:[p.baseOutlineScale,p.primaryRingScale,p.secondaryRingScale,p.phaseOverlayScale,p.alphaScale].every(v=>v>=0&&v<=1)});}return{samples,actionCount:ACTION_BUTTONS.length,presentationOnly:true as const,gameplayFormulaMutation:false as const,snapshotSchemaMutation:false as const,newAtlasCount:0 as const,passed:samples.length===18&&samples.every(s=>s.passed)&&ACTION_BUTTONS.length===9};}
