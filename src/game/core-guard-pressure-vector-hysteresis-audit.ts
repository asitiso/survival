import { ACTION_BUTTONS } from './config.js';import { CORE_GUARD_PRESSURE_VECTOR_HOLD_SECONDS,advanceCoreGuardPressureVectorHysteresis,createCoreGuardPressureVectorHysteresisState } from './core-guard-pressure-vector-hysteresis-rendering.js';
export function runCoreGuardPressureVectorHysteresisAudit(){const samples:{id:string;passed:boolean}[]=[];for(const reduced of [false,true])for(const incoming of [{x:1,y:0},{x:0,y:1},{x:-1,y:0},{x:0,y:-1}]){let s=advanceCoreGuardPressureVectorHysteresis(createCoreGuardPressureVectorHysteresisState(),{x:1,y:0},0,reduced);s=advanceCoreGuardPressureVectorHysteresis(s,incoming,CORE_GUARD_PRESSURE_VECTOR_HOLD_SECONDS*.25,reduced);samples.push({id:`${reduced}-${incoming.x}:${incoming.y}`,passed:Boolean(s.vector)&&Math.abs(Math.hypot(s.vector?.x??0,s.vector?.y??0)-1)<1e-6&&s.holdRemaining>=0});}return{samples,actionCount:ACTION_BUTTONS.length,presentationOnly:true as const,gameplayFormulaMutation:false as const,snapshotSchemaMutation:false as const,newAtlasCount:0,passed:samples.length===8&&samples.every(s=>s.passed)&&ACTION_BUTTONS.length===9};}
