import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';async function load(){return import('../dist/game/safe-lane-gap-feather-hysteresis-rendering.js');}const source=()=>fs.readFileSync(new URL('../src/game/game.ts',import.meta.url),'utf8');
test('phase 3483 a newly imminent route gap is adopted immediately so danger is never visually hidden',async()=>{const m=await load();const s=m.advanceSafeLaneGapFeatherHysteresisState(undefined,{start:.4,end:.6},1,false);assert.equal(s.visible,true);assert.ok(Math.abs(s.start-.4)<.001&&Math.abs(s.end-.6)<.001);});
test('phase 3484 moving hazard gap boundaries interpolate instead of jumping in one frame',async()=>{const m=await load();let s=m.advanceSafeLaneGapFeatherHysteresisState(undefined,{start:.3,end:.5},1,false);s=m.advanceSafeLaneGapFeatherHysteresisState(s,{start:.5,end:.7},1.016,false);assert.ok(s.start>.3&&s.start<.5);assert.ok(s.end>.5&&s.end<.7);});
test('phase 3485 disappearing hazard keeps a short visual release tail before clearing feather ownership',async()=>{const m=await load();let s=m.advanceSafeLaneGapFeatherHysteresisState(undefined,{start:.3,end:.5},1,false);s=m.advanceSafeLaneGapFeatherHysteresisState(s,null,1.04,false);assert.equal(s.visible,true);assert.ok(s.release>0);});
test('phase 3486 released safe-lane gap fully retires after the bounded memory window',async()=>{const m=await load();let s=m.advanceSafeLaneGapFeatherHysteresisState(undefined,{start:.3,end:.5},1,false);s=m.advanceSafeLaneGapFeatherHysteresisState(s,null,1.04,false);s=m.advanceSafeLaneGapFeatherHysteresisState(s,null,1.3,false);assert.equal(s.visible,false);});
test('phase 3487 reduced motion tracks current gap geometry directly without animated boundary lag',async()=>{const m=await load();let s=m.advanceSafeLaneGapFeatherHysteresisState(undefined,{start:.2,end:.4},1,true);s=m.advanceSafeLaneGapFeatherHysteresisState(s,{start:.6,end:.8},1.016,true);assert.equal(s.start,.6);assert.equal(s.end,.8);});
test('phase 3488 live safe-lane resets and advances feather hysteresis presentation-only',async()=>{const s=source();assert.match(s,/safeLaneGapFeatherHysteresisState/);assert.match(s,/advanceSafeLaneGapFeatherHysteresisState/);const a=await import('../dist/game/safe-lane-gap-feather-hysteresis-audit.js');const r=a.runSafeLaneGapFeatherHysteresisAudit();assert.equal(r.actionCount,9);assert.equal(r.presentationOnly,true);assert.equal(r.gameplayFormulaMutation,false);assert.equal(r.snapshotSchemaMutation,false);assert.equal(r.newAtlasCount,0);assert.equal(r.passed,true);});
