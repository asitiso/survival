import { ACTION_BUTTONS } from './config.js';import { bossSpecialAnticipationEmphasisPresentation } from './boss-special-anticipation-emphasis-rendering.js';import type { BossArchetype, BossPhase } from './boss-patterns.js';
export function runBossSpecialAnticipationEmphasisAudit(){const samples:{id:string;passed:boolean}[]=[];for(const archetype of ['inferno','summoner','juggernaut','abyssWitch','twinMaw','timeEater'] as BossArchetype[])for(const phase of [1,2,3] as BossPhase[])for(const reduced of [false,true]){const p=bossSpecialAnticipationEmphasisPresentation({archetype,phase,charge:.78,recovery:0,stagger:0},reduced,reduced);samples.push({id:`${archetype}-${phase}-${reduced}`,passed:p.active&&p.bodyStrength>=0&&p.bodyStrength<=1&&p.bodyScaleX>.7&&p.bodyScaleX<1.3&&p.bodyScaleY>.7&&p.bodyScaleY<1.3&&p.ringAlphaScale>=0&&p.ringAlphaScale<=1.35&&p.ringRadiusScale>=1});}return{samples,actionCount:ACTION_BUTTONS.length,presentationOnly:true as const,gameplayFormulaMutation:false as const,snapshotSchemaMutation:false as const,newAtlasCount:0 as const,passed:samples.length===36&&samples.every(s=>s.passed)&&ACTION_BUTTONS.length===9};}
