import { ACTION_BUTTONS } from './config.js';import { heroBodyFacingOwnerPresentation } from './hero-body-facing-owner-rendering.js';
export function runHeroBodyFacingOwnerAudit(){const samples:{id:string;passed:boolean}[]=[];for(const cast of [0,.8])for(const ultimate of [0,.8])for(const x of [-1,0,1]){const p=heroBodyFacingOwnerPresentation({currentFacing:{x:1,y:0},cast:{owner:cast>0?'cast':'current',facingX:x||1,facingY:x===0?1:0,retention:cast},ultimate:{owner:ultimate>0?'ultimate':'current',facingX:x||1,facingY:x===0?-1:0,retention:ultimate}},false);samples.push({id:`${cast}-${ultimate}-${x}`,passed:Number.isFinite(p.facingX)&&Number.isFinite(p.facingY)&&Number.isFinite(p.bodyAngle)&&Math.abs(Math.hypot(p.facingX,p.facingY)-1)<.001&&(p.mirrorX===1||p.mirrorX===-1)});}return{samples,actionCount:ACTION_BUTTONS.length,presentationOnly:true as const,gameplayFormulaMutation:false as const,snapshotSchemaMutation:false as const,newAtlasCount:0 as const,passed:samples.every(s=>s.passed)&&ACTION_BUTTONS.length===9};}
