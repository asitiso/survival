import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const u=new URL('../src/game/safe-lane-forecast-visual-coherence-rendering.ts',import.meta.url);const game=()=>fs.readFileSync(new URL('../src/game/game.ts',import.meta.url),'utf8');async function load(){assert.equal(fs.existsSync(u),true);return import('../dist/game/safe-lane-forecast-visual-coherence-rendering.js');}
test('phase 3285 safe-lane coherence exposes handoff detail lifecycle fields',async()=>{const m=await load();const p=m.safeLaneForecastVisualCoherencePresentation({currentTarget:{x:410,y:430},currentConfidence:.8,nextTarget:{x:410,y:430},forecastUrgency:.46,transitionMs:900},false);assert.equal(typeof p.forecastDetailVisible,'boolean');assert.equal(typeof p.arrivalAlphaScale,'number');assert.equal(typeof p.directionVisible,'boolean');});
test('phase 3286 settled handoff suppresses stale forecast detail and direction identity',async()=>{const m=await load();const p=m.safeLaneForecastVisualCoherencePresentation({currentTarget:{x:410,y:430},currentConfidence:.8,nextTarget:{x:410,y:430},forecastUrgency:.46,transitionMs:900},false);assert.equal(p.handoffSettled,true);assert.equal(p.forecastDetailVisible,false);assert.equal(p.directionVisible,false);});
test('phase 3287 settled handoff keeps one subdued arrival locator instead of a second full pulse',async()=>{const m=await load();const p=m.safeLaneForecastVisualCoherencePresentation({currentTarget:{x:410,y:430},currentConfidence:.8,nextTarget:{x:410,y:430},forecastUrgency:.46,transitionMs:900},false);assert.ok(p.arrivalAlphaScale>0&&p.arrivalAlphaScale<1);});
test('phase 3288 separated forecast keeps full arrival locator and detail visibility',async()=>{const m=await load();const p=m.safeLaneForecastVisualCoherencePresentation({currentTarget:{x:500,y:310},currentConfidence:.8,nextTarget:{x:410,y:430},forecastUrgency:.8,transitionMs:800,promotionOwner:'forecast'},false);assert.equal(p.forecastDetailVisible,true);assert.equal(p.directionVisible,true);assert.equal(p.arrivalAlphaScale,1);});
test('phase 3289 reduced flash cannot raise settled arrival emphasis',async()=>{const m=await load();const input={currentTarget:{x:410,y:430},currentConfidence:.8,nextTarget:{x:410,y:430},forecastUrgency:.46,transitionMs:900};const a=m.safeLaneForecastVisualCoherencePresentation(input,false),b=m.safeLaneForecastVisualCoherencePresentation(input,true);assert.ok(b.arrivalAlphaScale<=a.arrivalAlphaScale);});
test('phase 3290 live map transition scales arrival locator and forecast text through handoff lifecycle with presentation-only audit',async()=>{const s=game();assert.match(s,/arrivalAlphaScale/);assert.match(s,/forecastDetailVisible/);assert.match(s,/directionVisible/);const a=await import('../dist/game/safe-lane-forecast-visual-coherence-audit.js');const r=a.runSafeLaneForecastVisualCoherenceAudit();assert.equal(r.actionCount,9);assert.equal(r.presentationOnly,true);assert.equal(r.gameplayFormulaMutation,false);assert.equal(r.snapshotSchemaMutation,false);assert.equal(r.passed,true);});
